# agnostic-ai project config
#
# Author specs once. Emit per-CLI configs. Run `agnostic-ai sync`.

version: 1

# Source directories (relative to this file). Defaults shown.
sources:
  agents: agents
  skills: skills
  rules: rules
  hooks: hooks

# AI CLIs to emit configs for. Comment any out to skip.
targets:
  - claude        # Claude Code (Anthropic)
  - codex         # Codex CLI (OpenAI)
  - gemini        # Gemini CLI (Google)
  - cursor        # Cursor (cursor.com)
  - copilot       # GitHub Copilot
  - aider         # Aider (aider.chat)
  - cline         # Cline (VSCode extension)
  - windsurf      # Windsurf (Codeium)
  - continue      # Continue (continue.dev)

# Capability degradation: when a feature is unsupported, log warning instead of error.
on-unsupported: warn   # warn | error | silent
