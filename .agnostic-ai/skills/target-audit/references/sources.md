# Upstream sources per target

Canonical vendor docs for every registered target. Auditors fetch from
here instead of searching, which is the single biggest cost saver in a
run: a fixed, known list of pages beats discovery every time.

One `## <target>` section is required per registered target, each with a
`docs:` line and a `watch:` line.
`tests/integration/target_audit_sources_test.go` enforces that, so a new
adapter cannot merge without its vendor docs landing here too.

Every URL below returned HTTP 200 on **2026-09-19**. The eleven kiro.dev
URLs were the one exception then, blocked at the host with a 403; all
eleven were re-fetched successfully through the reader proxy on
**2026-09-20** (see the kiro section). Vendors move
doc hosts often (the Codex skills path moved twice in 2026), so a 404 is
itself a finding: record it as `docs-moved` and put the replacement URL
in the report so this file gets patched.

**A 200 is not a correct entry.** The 2026-09-19 run found four entries
whose URLs were live and whose named authority was dead, and each one had
hidden a real finding from earlier runs. A URL health check cannot catch
that class, so read the content of an entry you rely on, not its status
code. When an entry names a file, a symbol, or a page as the thing to
trust, check that the name still exists before trusting what it says.

`docs` = the pages describing the file formats agnostic-ai emits.
`changelog` = where new features land first; read it before the docs
when hunting for "what changed since last audit".

Some vendor sites render client-side (goose, kilo, antigravity, trae,
continue). `curl` returns 200 with an empty body there; use WebFetch or
WebSearch instead and do not conclude "page is empty" from a curl body.

**The worse case is a 200 that looks like content and is not.**
`ampcode.com/manual` returns 25 KB of SvelteKit app shell carrying none of
the manual's text (target-audit 2026-08-27). A byte count is not proof of a
successful fetch: grep the body for a string you expect before trusting it.

**Re-check every negative with `curl -sL` before reporting it.** A
WebFetch that contradicts a prior audit is more often a fetch failure
than a vendor change. The 2026-09-18 run produced three wrong negatives,
two of which would have shipped a false breaking finding. WebFetch does
not follow `<meta http-equiv="refresh">`, so a moved page reads as a
short body whose `.md` mirror 404s, which reads in turn as "the schema is
undocumented" when the vendor documents it fully at the new path.

**A soft 404 also passes a status check.** `cursor.com/docs/commands`
serves HTTP 200 with the full docs chrome and a "404 error code"
not-found graphic in the body (target-audit 2026-09-19). Grep the body
for the page's own heading before recording it as live, the same way the
`ampcode.com/manual` case demands.

**A blanket redirect is not a docs move.** `cursor.com` was unreachable
from the 2026-09-19 audit sandbox: TCP connect hangs and curl returns
`000`, while DNS resolves fine. Both fallback hosts, `docs.cursor.com`
and `www.cursor.com`, 308 every path to the bare `/docs` index, so they
confirm nothing about a specific page. Two of the four documented
fallbacks fail there for a network reason, not a content one, which is
exactly the setup for a future run to conclude the pages are gone.
`https://r.jina.ai/https://cursor.com/<path>` works.

Fetching tricks that each cost an auditor a wasted call, kept here so they
cost nobody again:

- Appending `.md` to a docs path serves a clean markdown mirror on
  **factory, qoder, augment, openhands, cline** (all Mintlify), on
  **antigravity** and **cursor** (confirmed on `/docs/hooks.md` and
  `/docs/skills.md`; not Mintlify, served from a Vercel `/api/raw`
  route), and on **kiro** (confirmed on `/docs/hooks/types.md`, which
  also links to it in-page as "View as Markdown"). It does **not** work
  on kilo or trae. On antigravity it covers the `/docs/` tree only:
  `https://antigravity.google/changelog.md` 404s (2026-09-11).
- **amp**: the raw-markdown mirror moved. `llms.txt` now serves every page at
  `https://ampcode.com/docs/markdown/<path>`, and the old
  `https://ampcode.com/docs/<path>/markdown` form 404s (confirmed both ways
  2026-09-20). `https://ampcode.com/llms.txt` still indexes every docs page in
  one call and states the rule itself. The count moves (50 in August, 53 on
  2026-09-19), so re-count it rather than trusting a number recorded here.
- **junie**: `https://junie.jetbrains.com/docs/HelpTOC.json` returns the full
  page list in one call. That is how three uncited pages were found.
- **trae**: neither WebFetch nor `.md` works (`ide/rules.md` returns 1.8 MB of
  SPA shell). Content is server-side inside `window._ROUTER_DATA` as a Quill
  delta: curl to disk, brace-match the object, collect `ops[].insert` per
  `zoneId` (table cells arrive as separate zones). Per-page `updated_at`
  lives in the same object, under `busStructure`: walk it to date a page
  without diffing its text. The changelog is separate
  and easy: `https://www.trae.ai/api/changelog` returns JSON.
- **kiro**: Next.js server-rendered, so plain curl plus a tag-strip gives full
  text. WebFetch truncates the 280 KB pages.
- **kilo**: `kilo.ai` is client-rendered and `.md` append 404s. So do
  `kilo.ai/api/raw-markdown?path=...` and `kilo.ai/api/llms.txt`, on a
  direct GET, even though both exist as route files (2026-09-11). The
  `Kilo-Org/kilocode` `packages/kilo-docs/pages/**` mirror is the only
  route; `gh api search/code` scoped to that path finds which page names
  a string.
- **continue**: no `.md` mirror and no `llms.txt`. Use `continuedev/continue`
  at `docs/customize/deep-dives/*.mdx`.
- **`docs.qoder.com/llms.txt` needs `curl --compressed`**, or the response is
  not valid UTF-8 and reads as garbage.
- When a Mintlify site seems to have no changelog, read its `llms.txt` first:
  factory's is listed there under `## Changelog` but absent from the nav.

---

## claude

- docs: https://code.claude.com/docs/en/memory (rules) · /hooks · /sub-agents · /skills (slash-commands merged in; `.claude/commands/` still works) · /mcp · /settings (prose on file precedence and reload) · /settings-reference (the settings **key** table; that is the page an auditor needs, and it is a different page from /settings, both 200 as of 2026-09-11)
- changelog: https://github.com/anthropics/claude-code/blob/main/CHANGELOG.md
- watch: `.claude/rules/` native loading, settings.json key surface, plugin/marketplace keys. Also the `.mcp.json` per-server field set: `headersHelper`, `timeout`, `alwaysLoad`, and the `oauth` object (`clientId`, `callbackPort`, `scopes`, `authServerMetadataUrl`), all documented at `/docs/en/mcp` and all emitting since #634 behind `emit.WithClaudeMCPExtras()`. Three audits read past this because this line named only rules, settings, and plugin keys. `oauth.clientSecret` is deliberately not emitted: `claude mcp add-json` takes the secret as a separate `--client-secret` flag and the page says it "is stored securely in your system keychain (macOS) or a credentials file, not in your config", so it is not a `.mcp.json` field. Re-check that if the page ever shows a secret inside the JSON block.
- verified 2026-09-20 (#955): **when the settings reference and the changelog disagree, prefer the changelog**, and check whether the reference page uses its own deprecation convention on the key in question. `/docs/en/settings-reference` still carries a live `### taskOutputMaxChars` section describing the removed `TaskOutput` tool, with no deprecation marker, while the CHANGELOG under **v2.1.277** reads "Removed the deprecated TaskOutput tool; Claude reads a background task's output file with Read instead, and the `taskOutputMaxChars` setting and `TASK_MAX_OUTPUT_LENGTH` no longer have any effect". That same page does mark `keybindingFlavor` "Deprecated since v2.1.261 and has no effect", so an omitted marker reads as **stale**, not as a live contradiction. Re-read that section next pass. **Date a CHANGELOG entry by parsing the `## <version>` headings, never by reading a summary of the page**: that number came back wrong twice, 2.1.275 from a summarised fetch and 2.1.278 from a flat text dump where the 2.1.278 and 2.1.277 entry lists ran together. The entry is line 78 and the nearest preceding heading is `## 2.1.277` at line 8. `bashOutputMaxChars` is unaffected; the entry names only the task key and `TASK_MAX_OUTPUT_LENGTH`. We keep emitting `taskOutputMaxChars` because npm `stable` was 2.1.267 that day, below 2.1.277, so the key still works there; the docs mark it deprecated-no-op instead.
- verified 2026-09-20: **`.claude/settings.json` is not claude-only.** Copilot CLI reads it for a five-key repository subset, so `enabledPlugins` and `hooks` written for claude also set Copilot CLI policy (see the copilot section, #956). Two more reads from `/docs/en/memory` worth not re-deriving: "**Not read**: `AGENTS.local.md`, `AGENTS.override.md`, or anything under a `.agents/` directory", which matters because codex, amp, zed, crush and openhands all put skills under `.agents/`; and `InstructionsLoaded` hooks "Don't fire" for an `AGENTS.md` read through the setting, though "They fire as usual for an `AGENTS.md` that a `CLAUDE.md` imports or symlinks to".
- verified 2026-09-12: [Stable hook handlers](https://code.claude.com/docs/en/hooks#hook-handler-fields): command, HTTP, MCP-tool, and prompt handlers now emit and import (#767). Watch common fields separately from command-only args, shell, and background options. Experimental agent hooks remain excluded.
- verified 2026-09-19 (#885, #893): **Claude Code reads AGENTS.md.** [/docs/en/memory#agents-md](https://code.claude.com/docs/en/memory#agents-md): "By default, Claude reads `AGENTS.md` only when you have no `CLAUDE.md` in your working directory or above it." [/docs/en/memory#when-claude-code-reads-agents-md](https://code.claude.com/docs/en/memory#when-claude-code-reads-agents-md): "**At session start**: every `AGENTS.md` and `.claude/AGENTS.md` in your working directory and the directories above it." Shipped in claude-code CHANGELOG **v2.1.277**: "Added AGENTS.md support: in a project with no CLAUDE.md, Claude Code reads AGENTS.md instead; change it under 'Project instructions' in `/config` (not yet on Bedrock, Vertex or Foundry)". Two consequences this repo now depends on, so re-read both on every pass. First, **claude must always write CLAUDE.md**, in every layout including an `outputs.claude.rules-file` pointed elsewhere: without it a repo with codex enabled hands Claude Code codex's AGENTS.md, rule bodies routed away from claude included (#885). The user toggle cannot substitute: it is `pluginConfigs["agents-md@builtin"].options.instructionFiles`, four values, default `claude-md-or-agents-md` (fallback **on**), scope "User or managed", and the vendor states "Claude Code ignores it in project and local settings files." This repo emits project tier only. Second, `import claude` walks CLAUDE.md, `.claude/CLAUDE.md`, AGENTS.md, `.claude/AGENTS.md` (#893); the root AGENTS.md rung yields to codex, amp, warp, crush, kiro, or opencode in the same run. Watch for the fallback default flipping, for the "not yet on Bedrock, Vertex or Foundry" carve-out closing or widening, for the toggle gaining project scope (which would let us write it), and for `.claude/AGENTS.md` leaving the read list. `internal/cli/doctor_unmanaged.go`'s `{"AGENTS.md", "codex"}` row stays single-owner on purpose: that table has no multi-target support and a wrong second owner is worse than a narrow one.
- watch: **"Claude Code mods"**, in case it ships under that name. The vendor announcement called the AGENTS.md fallback "built off of our new Claude Code mods feature", and the name is nowhere in the docs (checked 2026-09-19): zero word-boundary matches in the v2.1.278 CHANGELOG, zero in `code.claude.com/docs/llms.txt`, zero on the memory, settings-reference and skills pages, and `/docs/en/mods` 404s. What the docs name for the mechanism is the built-in `agents-md` plugin under the existing `pluginConfigs` key, an established surface and user-or-managed tier. So "mods" is unreleased, internal, or shorthand. Do not file on it. If it ships as a real surface it is plausibly a new spec kind worth modelling.

## codex

- docs: https://learn.chatgpt.com/docs/build-skills · /docs/agent-configuration/subagents · /docs/custom-prompts · /docs/hooks · /docs/config-file/config-reference · /docs/agent-configuration/rules (exec-policy precedence, not AGENTS.md discovery) · /docs/agent-configuration/agents-md (AGENTS.md discovery) · /docs/permissions (beta Permission Profiles, filesystem and network only)
- changelog: https://learn.chatgpt.com/docs/changelog
- watch: skills dir has moved twice (`.codex/skills` -> `.agents/skills`); prompts are deprecated in favour of skills; hooks JSON event names. Codex CLI 0.152.0 (2026-09-01, changelog) widened the MCP server-name charset to `:`, `@`, `/`, `.` for package-style names (openai/codex#41700, "Support package-style MCP server names"); `config.toml` server-name and per-tool/oauth sub-table headers now quote a non-bare name via `tomlKeySegment` (#706, confirmed 2026-09-09). **The package-style-name round-trip gap this line used to describe is closed** (verified 2026-09-19). It named `internal/spec.checkSpecName` as the blocker. That symbol no longer exists anywhere in the tree, and grepping for it is a trap: the only hits are stale copies under `.claude/worktrees/`, which look live. The live check is `spec.ValidateName` (`internal/spec/spec.go:686`), which special-cases MCP and **permits** `/`: "MCP names may contain forward slashes because vendor identifiers use them and MCPFileName encodes per-server filenames." `writeCodexMCPs` (`import_codex_native.go:1057`) builds the filename through `spec.MCPFileName(name)`, not `name+".yaml"`. A `config.toml` carrying `["mcp_servers"."npm:@modelcontextprotocol/server-sequential.thinking"]` round-trips through `import codex` and `sync` byte-identically. Do not re-file it.
 **Three permission surfaces, do not conflate them** (target-audit 2026-09-19, #923): `approval_policy` in `config-reference` is one global mode, never a rule list. `/docs/permissions` beta Permission Profiles map filesystem paths and network domains, and have **no ask verb**. Only exec-policy `prefix_rule(pattern=[...], decision="allow"|"prompt"|"forbidden")` under `/docs/agent-configuration/rules` is shaped like the portable lists, and **this repo already writes it** through `outputs.codex.exec-policies`, decoupled from `Settings.permissions`. So a note claiming Codex has no permission key would be false, and a translation would have to guess how to split a command string into `prefix_rule`'s token list. Both the rules and profiles pages carry vendor experimental warnings.
## gemini

- docs: https://geminicli.com/docs/hooks/reference/ · https://geminicli.com/docs/core/subagents.md · https://geminicli.com/docs/reference/tools.md · https://geminicli.com/docs/cli/skills/ · /docs/cli/custom-commands/ · /docs/reference/configuration · /docs/cli/gemini-ignore/ (the ignore file Gemini CLI actually reads is `.geminiignore`; `.aiexclude` belongs to Gemini Code Assist, a different product, target-audit 2026-08-27)
- changelog: https://github.com/google-gemini/gemini-cli/releases
- watch: Hook definitions require a nested `hooks` array; the stable v0.59.0 loader discards flat entries (https://github.com/google-gemini/gemini-cli/blob/v0.59.0/packages/core/src/hooks/hookRegistry.ts#L244, #762). Handler timeouts are milliseconds; `sequential` belongs to the definition. `.gemini/skills/` vs the `.agents/skills` alias precedence, `settings.json` hooks + mcpServers schema. `/docs/core/subagents.md` documents the native project subagent directory `.gemini/agents/*.md` with a full frontmatter table (`name` and `description` required, then `kind`, `tools`, `mcpServers`, `model`, `temperature`, `max_turns`, `timeout_mins`), emitted since #733; watch that table for a new field and watch whether `kind: remote` ever needs more than a passthrough. Two vocabularies diverge here and must not be conflated: subagent `tools` takes **Gemini's own** snake_case tool names from `/docs/reference/tools.md` (`run_shell_command`, `replace`, `grep_search`, ...), not Claude's, so `geminiToolName` in `internal/adapters/gemini/agents.go` translates the eight generic names and drops the rest with a coverage note. Re-check that reference table whenever Gemini renames a tool (`grep_search` already carries `search_file_content` as a "legacy alias"). The pre-#733 slash-command TOML per agent survives behind `outputs.gemini.emit-agents-as-commands`.

## cursor

- docs: https://cursor.com/docs/skills · /docs/subagents · /docs/rules · /docs/hooks · /docs/mcp · /docs/bugbot · /docs/reference/third-party-hooks.md (the Claude Code hook-compatibility page; uncited until target-audit 2026-09-12, #756) · https://cursor.com/help/customization/skills.md (the old `/docs/agent/chat/commands` 308s here, to a "migrate commands to skills" FAQ; no Cursor page documents `.cursor/commands` any more, target-audit 2026-08-27)
- changelog: https://cursor.com/changelog
- watch: `.mdc` frontmatter fields, hooks event names (camelCase, e.g. `beforeShellExecution`), environment.json schema. Cursor also loads Claude Code's hook file: `/docs/reference/third-party-hooks.md` ranks `.claude/settings.json` 6th and `.cursor/hooks.json` 3rd in one seven-rank merge, and states "All matching hooks from every source run. When responses conflict, higher-priority sources take precedence during merge." The same page maps each Claude event name onto its Cursor equivalent (`PreToolUse` onto `preToolUse`, and so on), so a repo syncing claude and cursor together runs every hook twice. Both gates disappeared, exactly as this line said to watch for, and the replacement is on: "Claude Code hooks load when **Include Third-Party Plugins, Skills, and Other Configs** is enabled in Cursor Settings → Agents → Third-Party Imports. The setting is on by default." (confirmed by curl 2026-09-18, #865). Watch for that default flipping, for the setting being renamed again, and for the priority table changing `.cursor/hooks.json`'s rank. One thing stays unsettled and must not be asserted either way: whether a Cursor-native camelCase spelling placed inside `.claude/settings.json` also fires. The mapping table covers the PascalCase direction only. `/docs/mcp.md`'s stdio field table marks `type` required ("**type** | Yes | Server connection type | `"stdio"`") and every JSON example on the page carries it, so cursor's stdio entries now emit it (target-audit 2026-09-19, #895). **That field is cursor-only and must stay that way:** Claude Code documents the opposite, "Claude Code reads an entry with no `type` as a stdio server", and the same shared builder serves claude, kiro, junie, qoder, factory, and copilot's root mirror.
- verified 2026-09-20 (#957): **Cursor loads skills from Claude's and Codex's directories too, by default.** `/docs/skills`, "Skill directories": "For compatibility, Cursor also loads skills from Claude and Codex directories: `.claude/skills/`, `.codex/skills/`, `~/.claude/skills/`, and `~/.codex/skills/`." It is gated by the same switch as the hooks cross-read, which names Skills in its own title: "Claude Code hooks load when **Include Third-Party Plugins, Skills, and Other Configs** is enabled in Cursor Settings → Agents → Third-Party Imports. The setting is on by default." So a repo syncing claude, codex and cursor has one skill read from three project roots. **Record that as "read from three roots, precedence undocumented", never as "loaded three times"**: no Cursor page says whether it dedupes across them, and the non-merging sentence on that page is about **Codex's** loader, not Cursor's, so it must not be borrowed to make the stronger claim. Watch for a precedence rule appearing and for the compatibility list gaining a root. Separately, `/docs/mcp`'s "Protocol and extension support" table rows **Roots** as "Supported", "Server-initiated inquiries into URI or filesystem boundaries". That is the MCP protocol capability, **not** a per-server config key: neither per-server field table has a `roots` row, and "Config interpolation" names the resolvable fields as "`command`, `args`, `env`, `url`, and `headers`". A capability matrix is easy to misread as confirming a config field, and `cursor.md` did until 2026-09-20.
- **`cursor.com/docs/<page>.md` now times out on curl** rather than returning an empty body (2026-09-20), so it reads as a dead host when it is a rendering problem. `https://r.jina.ai/https://cursor.com/docs/<page>` works and returns clean markdown.
- verified 2026-09-12: [Prompt hooks](https://cursor.com/docs/hooks.md#prompt-based-hooks) now emit type, prompt, optional model, and native common options (#768). Cursor command entries retain their existing shape.

## copilot

- docs: https://docs.github.com/en/copilot/reference/custom-agents-configuration · https://docs.github.com/en/copilot/how-tos/copilot-on-github/customize-copilot/customize-cloud-agent/add-skills · https://code.visualstudio.com/docs/agent-customization/mcp-servers (MCP; VS Code's docs are authoritative for `.vscode/mcp.json`, docs.github.com does not cover it) · https://docs.github.com/en/copilot/how-tos/copilot-cli/customize-copilot/add-mcp-servers (the Copilot CLI MCP page; this is the one that names the accepted top-level keys) · https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-config-dir-reference (Copilot CLI user, repository, and local settings plus repository model policy) · https://docs.github.com/en/copilot/concepts/agents/hooks · https://docs.github.com/en/copilot/reference/hooks-reference (14 events) · https://docs.github.com/en/copilot/how-tos/copilot-on-github/customize-copilot/customize-cloud-agent/use-hooks (the default-branch requirement)
- changelog: https://github.blog/changelog/label/copilot/
- watch: `.github/instructions/*.instructions.md` `applyTo` semantics, agent frontmatter keys. The [Copilot CLI configuration directory](https://docs.github.com/en/copilot/reference/copilot-cli-reference/cli-config-dir-reference) is the canonical source for `.github/copilot/settings.json`, `.github/copilot/settings.local.json`, and `.github/allowed_models.txt`; watch its precedence order, repository-supported keys, and model allowlist format. Enable/disable state is stored outside `mcp.json`, so there is no per-server `disabled` key. **The MCP file question is settled and the answer is not what this line used to say** (target-audit 2026-08-27): Copilot CLI reads `.mcp.json` and `.github/mcp.json`, under either an `mcpServers` object or the bare top-level format, and does **not** read `.vscode/mcp.json` — "It uses the unsupported top-level key `servers`." VS Code forwards its own config to the Agent Host, so a VS Code user is unaffected; a CLI-only user gets nothing. This adapter now writes `.github/mcp.json` (the CLI's committed-config path, `mcpServers` key) alongside the VS Code file, and keeps the root `.mcp.json` opt-in behind `outputs.copilot.root-mcp-file` since the project root is shared ground with claude (#646). Watch for a VS Code or GitHub page stating the accepted top-level key for the `.mcp.json` Agent Host reads, and for a cloud-agent MCP page naming `.github/mcp.json`; both remain open and would widen the fix. **Hooks landed in #629** (`.github/hooks/*.json`, verified 2026-09-10): the "Hook events" table lists **14** events today, one more than the 13 recorded when #629 was filed (`userPromptTransformed` has no PascalCase pairing documented anywhere on the page, so it likely landed after that count was taken — re-check the row count on the next pass). `timeoutSec` is the field name (`timeout` is a deprecated alias); the wrapper is `{"version": 1, ...}` with an **integer** version, confirmed from the page's own policy-hook example, not the quoted string `"v1"` Kiro's wrapper needed #626 to fix. The page documents two independently valid event-name-casing forms for the *same* config file — camelCase (native) and PascalCase ("VS Code compatible", which also carries Claude's own matcher semantics and tool names) — so this adapter passes `event:` through verbatim rather than translating, the same policy every other hook emitter in this repo already follows; watch for either form's docs section (`camelCase / PascalCase` pairs under "Hook event input payloads") losing a pairing or adding a fifteenth event. **The both-casings rule does not cover every row** (target-audit 2026-09-11, #737): two of the 14 are camelCase-only, `userPromptTransformed` and `subagentStart` ("A subagent is spawned (before it runs)."), so a cross-target spec written as `SubagentStart` (legal on claude and codex) emits a Copilot key that parses and never fires. The page carries 12 PascalCase spellings in total: ten `camelCase / PascalCase` headings, plus `Notification` (its own `hook_event_name: "Notification"` payload) and `PermissionRequest` (the Claude-format matchers note). Count those 12 against the table's 14 rows on every pass, and watch for either single-spelling event gaining a pairing. **Hook sources combine, and one of them is a file we also write** (target-audit 2026-09-12, #755): "Hooks are loaded from the following sources in order (policy, then user, then project, then plugins) and combined. When the same event appears in multiple sources, all hook entries from all sources are run." The same list names `.github/hooks/*.json` and, under the inline-settings entry, "Cross-tool `.claude/settings.json` and `.claude/settings.local.json` files in the repository are also read." claude and copilot are both in `DefaultTargets()`, so one hook spec targeting both runs twice under Copilot CLI out of the box, and the page names no toggle for that read, unlike cursor's and trae's. Watch for one appearing. Two more sentences from the same page gate the `exec`/`args` form emitted since #755: both fields are marked "Only supported in Copilot CLI", and a cloud agent job reads the very same `.github/hooks/*.json` files under a narrower rule, "A subset of events fires, and only `bash` (or `command`) entries are honored", so an exec-form entry is inert there. Watch for cloud agent gaining `exec` support, which would retire that adapter's coverage note. Four more from target-audit 2026-09-19 (#888). `SubagentStart` is gone from the validator's event table: that table is a **second list**, separate from the emitter's `hookLifecycle`, and only the emitter carried the camelCase-only caveat, so `validate` vouched for a key Copilot parses and never fires. Check both lists whenever the event table changes. The command-hook field table's `cwd` and `env` now emit and import; neither is on Claude Code's own command-hook table, so both stay copilot-scoped. The repository-settings table's `disabledMcpServers` (`string[]`, "Union—repository can add entries, never remove") is the file-based disable route the MCP coverage note used to deny existed, and it lives in `.github/copilot/settings.json`, a file this adapter already merges into; the note now names the VS Code reader only. The CLI MCP page's per-server `tools` allowlist ("Enter `*` to include all tools, or provide a comma-separated list of tool names") emits on `.github/mcp.json` and the root mirror. **Do not share that one with codex**: codex's `tools` is a map of per-tool sub-tables (`output_token_limit`, `approval_mode`), a different shape under the same key. **Permissions are MDM-only** (target-audit 2026-09-19, #917): the repository-settings table on `cli-config-dir-reference` is closed, "Any other keys, including keys that are valid in the user configuration file, are silently ignored", and its fourteen keys name no `permissions`. Copilot's deny/ask/allow grammar (`Bash`, `Read`, `Edit`, `Domain`) is real but lives only in device-level MDM managed settings, at paths like `/etc/github-copilot/managed-settings.json`, outside any repository. The user-tier `~/.copilot/permissions-config.json` is not a fallback: the vendor says it "doesn't support deny rules, 'ask' rules, default modes, URL rules, tool filtering, or repository-local shared policy". The adapter now raises a coverage note. Watch that fourteen-key table for a `permissions` entry.
- verified 2026-09-20 (#956): **the `.claude/settings.json` cross-read is wider than hooks**, so do not re-derive it from the hooks page alone. `/reference/copilot-cli-reference/cli-config-dir-reference` names a five-key subset: "The CLI also reads `.claude/settings.json` and `.claude/settings.local.json` for the shared cross-tool subset of repository settings (such as `companyAnnouncements`, `disableAllHooks`, `enabledPlugins`, `extraKnownMarketplaces`, and `hooks`)." Two of the five are keys this repo writes: `enabledPlugins`, through claude's first-class settings, and `hooks`, through either target. Watch that list for a sixth key. **A decoy to close rather than re-open**: there is no project-level `.copilot/settings.json`. A WebSearch summary asserts one; all four mentions of that filename on the page are `~/.copilot/settings.json`, user tier, and the precedence list spells the repository file as `.github/copilot/settings.json`, which is what this adapter writes. **A counting trap on the hooks page**: `agentStop`'s PascalCase pairing is **`Stop`**, not `AgentStop`. The heading reads "agentStop / Stop" and the payload field is `hook_event_name: "Stop"`, so an auditor grepping `AgentStop` finds zero and wrongly concludes a pairing was dropped. **No VS Code page documents a per-server `roots` key either**: the string returns zero hits on the whole of `code.visualstudio.com/docs/agents/reference/mcp-configuration`, the page `copilot.md` cited as the authority for it (135 KB, real content, checked 2026-09-20). That claim stood on the claude, cursor and copilot pages at once, all three describing the one shared builder at `emit/mcp.go`'s `BuildRoots` call, which is its only call site. When a field claim repeats across targets sharing a builder, check the emitter and every page it feeds, not the one page in front of you.
- verified 2026-09-12: [VS Code MCP configuration](https://code.visualstudio.com/docs/agents/reference/mcp-configuration#development-mode) documents dev.watch for all transports and dev.debug for stdio only (#769). Preserve top-level inputs and sandbox while replacing servers; dedicated CLI and root mirror files remain wholly managed (#757).

## aider

- status: dormant. Default branch `main` last moved 2026-05-22 (sha 5dc9490, repo `pushed_at` agrees); newest tag is v0.86.2 / v0.86.3.dev, both 2026-02-12. Note `releases/latest` returns v0.86.0 (2025-08-09) and is misleading: Aider stopped publishing GitHub Release objects after v0.86.0 but kept tagging, so read tags, not releases. aider.chat is a Jekyll site inside the repo at `aider/website/`, so the docs cannot change without a commit — one `gh api repos/Aider-AI/aider --jq '.pushed_at'` bounds the whole target. Re-open the full sweep if `pushed_at` moves past 2026-05-22. Still dormant on 2026-09-20: `main` is 5dc9490 dated 2026-05-22 and `pushed_at` agrees, so the bound has not moved in four months.
- docs: https://aider.chat/docs/usage/conventions.html · https://aider.chat/docs/config/aider_conf.html
- changelog: https://aider.chat/HISTORY.html
- watch: whether Aider gained any per-file rules/skills surface (today it is conventions + `.aiderignore` only).

## cline

- docs: https://docs.cline.bot/customization/cline-rules · /customization/skills · /getting-started/config (append `.md` to any path for a clean markdown mirror; `llms.txt` lists every page)
- source of truth for project paths: `sdk/packages/shared/src/storage/paths.ts` in `cline/cline`, the resolver functions `resolveWorkspaceRulesConfigPaths`, `resolveSkillsConfigSearchPaths`, `resolveAgentConfigSearchPaths`, `resolveHooksConfigSearchPaths` and `resolveWorkflowsConfigSearchPaths`. **Read it before trusting `/getting-started/config`**, whose project tree contradicts the surfaces it documents. `gh api repos/cline/cline/contents/sdk/packages/shared/src/storage/paths.ts --jq '.content' | base64 -d` prints it.
- secondary, and partly dead: `apps/vscode/src/core/storage/disk.ts`, the `GlobalFileNames` block. It was the authority here until 2026-09-19 and is no longer one. `GlobalFileNames.clineRules` has **zero call sites** (`gh api search/code -f q='repo:cline/cline "GlobalFileNames.clineRules"'` returns 0), so a reader who stops at this file sees `.clinerules` as the single rules path and misses the second layout every surface honors. Reading only `disk.ts` hid all four cline findings of the 2026-09-19 run, including the only breaking one. Cite it for what the VS Code extension alone resolves (`workflows: ".clinerules/workflows"` is still live there), never for what Cline reads.
- changelog: https://github.com/cline/cline/releases
- watch: the resolver list in `paths.ts` for a project path we do not write. **Corrected 2026-09-19, and the correction is the lesson**: project rules live in two layouts, not one. `resolveWorkspaceRulesConfigPaths` (`paths.ts:528`) returns `<workspace>/.clinerules` **and** `<workspace>/.cline/rules`, over a doc comment reading "Every Cline surface (CLI, VS Code extension, desktop app) must honor both — hosts that hardcode one of them silently drop the other's rules (cline/cline#14186)". `docs.cline.bot/customization/cline-rules` agrees: "Both layouts are supported by VS Code, Desktop, and the CLI" / "Both directories are searched when present". The reading behind #853, that `/getting-started/config` invents `.cline/rules` and no loader reads it, came from counting `GlobalFileNames` hits and is now wrong. `.clinerules` stays our default because the VS Code Rules panel still creates there, not because `.cline/rules` goes unread. Do **not** call `.clinerules` deprecated in user-facing text either: the SDK constant is spelled `DEPRECATED_CONFIG_DIR`, but `docs.cline.bot/resources/deprecations.md` lists exactly three items, `.clineignore`, "Explain Changes" and "Focus Chain", and `.clinerules` is not among them. `.cline/agents/` is settled in both directions as of 2026-09-19, so there is nothing left to run a check on: `resolveAgentConfigSearchPaths` (`paths.ts:476`) returns `<workspace>/.cline/agents`, so the **path is confirmed**, and all three shipping readers filter that directory to `.yml`/`.yaml` and require a `---` frontmatter block carrying `name` and `description` (`sdk/packages/core/src/extensions/tools/team/configured-agent-config.ts` `isYamlFile` plus `ConfiguredAgentFrontmatterSchema`, repeated in `apps/cline-hub/src/server/user-instructions.ts` and `apps/cli/src/tui/interactive-config.ts`), so the **format we write is wrong**. Watch `isYamlFile` and that zod schema, not the path. `.cline/skills/` is confirmed by `resolveSkillsConfigSearchPaths`, which since 2026-09-19 also scans `.clinerules/skills` and `.agents/skills` (`getWorkspaceSkillDirectories`, `paths.ts:461`; `getSkillsDirectoriesForScan` in `apps/vscode/src/core/storage/skill-directories.ts` agrees independently). Import reads all four since #889; emission stays at `.cline/skills`, the documented default, with `outputs.cline.skills-dir: .agents/skills` for a project that wants the shared tree. Watch that list for a fifth entry. Also watch `.cline/plugins/` (listed in the config reference, unemitted, no confirmed schema); MCP config location. **`.cline/cron/` comes off this watch list** (2026-09-20): `paths.ts` declares `resolveWorkspaceCronSpecsDir` under the comment "Workspace file-based cron spec authoring directory reserved for future workspace-scoped automation support", and `ResolveCronSpecsDirOptions.scope` "Defaults to `global`, i.e. `~/.cline/cron`". So the workspace path exists as a reserved function with no default caller, which is not a surface to emit to. Re-open it only when the scope default moves or a reader calls that function. `docs.cline.bot/features/workflows` still 404s and `llms.txt` lists no project-scoped replacement (target-audit 2026-08-08, #563), but the surface itself is settled in source and the "unconfirmed export" framing came off in #889. `resolveWorkflowsConfigSearchPaths` (`paths.ts:595`) returns `<workspace>/.clinerules/workflows` **and** `<workspace>/.cline/workflows`, and only the first reaches the VS Code extension: `apps/vscode/src/core/context/instructions/user-instructions/workflows.ts` resolves `GlobalFileNames.workflows`, which `disk.ts:24` spells `.clinerules/workflows`. That is the path to recommend. The same file's `CLINERULES_EXCLUDED_SUBDIRECTORIES` (`cline-rules.ts:11-15`) excludes `.clinerules/workflows`, `.clinerules/hooks` and `.clinerules/skills` from the rules scan, so none of the three doubles as an always-on rule. Watch whether `.cline/workflows` gains a VS Code reader. `.clineignore` is now vendor-flagged deprecated (`llms.txt` titles it "`.clineignore` (deprecate soon)"); no impact, cline declares no `KindIgnore`. **The hooks decline reason no longer holds** (target-audit 2026-09-19). It read "`/customization/hooks` is a one-line stub pointing at SDK Plugins, where hooks are a TypeScript `AgentPlugin` API, not shell-command-on-event". Cline now ships one executable script per event: `sdk/packages/core/src/hooks/hook-file-config.ts` declares ten event file names (`TaskStart`, `TaskResume`, `TaskCancel`, `TaskComplete`, `TaskError`, `PreToolUse`, `PostToolUse`, `UserPromptSubmit`, `PreCompact`, `SessionShutdown`) and a `SUPPORTED_HOOK_FILE_EXTENSIONS` set (`""`, `.sh`, `.bash`, `.zsh`, `.js`, `.mjs`, `.cjs`, `.ts`, `.mts`, `.cts`, `.py`, `.ps1`), and `hook-file-hooks.ts` runs them through `runSubprocessEvent`. The two project paths that used to disagree both resolve: `resolveHooksConfigSearchPaths` (`paths.ts:487`) pushes `<workspace>/.clinerules/hooks` and `<workspace>/.cline/hooks`. The docs page is still a stub, so this is source-confirmed and doc-unconfirmed; watch for `/customization/hooks` to grow real content. Emitted since #889 as one executable script per event under `.cline/hooks`; watch `HookConfigFileName` for an eleventh name and `HOOK_CONFIG_FILE_EVENT_MAP` for `PreCompact` gaining a runtime event (it maps to `undefined` today, so a `PreCompact` file is listed and never run). Hooks also resolve at `~/.cline/hooks` and a Documents "Hooks" path, both left to the user-scope pass. Rule activation emits `paths` since #639; `/customization/cline-rules` names it as the only conditional ("Currently, `paths` is the supported conditional"), so watch for a second one, particularly anything description-driven, since a rule with `alwaysApply: false` and no globs has no representation today.
- **the skills doc and the source disagree in both directions** (2026-09-20), and neither gap affects us. `/customization/skills.md` lists `.cline/skills/`, `.clinerules/skills/` and `.claude/skills/`; `getWorkspaceSkillDirectories` (`paths.ts:461`) maps `DEPRECATED_CONFIG_DIR` (`.clinerules`), `CLINE_CONFIG_DIR` (`.cline`) and `LEGACY_AGENT_SKILLS_CONFIG_DIR` (`.agents`). So `.claude/skills` is doc-only and `.agents/skills` is source-only. We emit to `.cline/skills`, which both name, and import reads all four, so the discrepancy is inert. Recorded because the next auditor will trip on it and spend a run deciding which side is authoritative.
- verified 2026-09-19 (#886): the agent file format is `sdk/packages/core/src/extensions/tools/team/configured-agent-config.ts` in `cline/cline`, not a doc page. `isYamlFile` accepts `.yml` and `.yaml` only (L113-115) and the scan skips everything else (L176-178); the content must match `/^(---)[^\S\r\n]*(?:\r?\n|$)/` or the loader throws "Missing YAML frontmatter block in agent config file." (L49-52); `ConfiguredAgentFrontmatterSchema` requires `name` and `description`, both `z.string().trim().min(1)`, with `tools`/`skills`/`providerId`/`modelId`/`maxIterations` optional (L8-10). The same filter is repeated in `apps/cline-hub/src/server/user-instructions.ts` L63-67 and `apps/cli/src/tui/interactive-config.ts` L219-226. Watch that zod object for a new required key: every one of them is a silent drop, since a file that fails the schema lands in `errors` and the agent simply is not there. Releases #534 through #886 wrote `<name>.md` with no frontmatter and reached none of the three surfaces.

## windsurf

- docs: https://docs.devin.ai/desktop/cascade/memories · https://docs.devin.ai/desktop/cascade/mcp · https://docs.devin.ai/desktop/cascade/agents-md · https://docs.devin.ai/cli/subagents · https://docs.devin.ai/cli/reference/permissions (the complete `allowed-tools` vocabulary) · https://docs.devin.ai/cli/extensibility/hooks/overview · https://docs.devin.ai/desktop/cascade/skills · https://docs.devin.ai/desktop/cascade/workflows · https://docs.devin.ai/desktop/context-awareness/windsurf-ignore · https://docs.devin.ai/desktop/devin-local · https://docs.devin.ai/cli/extensibility/rules · https://docs.devin.ai/cli/extensibility/skills/overview · https://docs.devin.ai/cli/extensibility/skills/creating-skills.md (the full SKILL.md frontmatter reference) · https://docs.devin.ai/cli/extensibility/mcp/configuration
- changelog: https://docs.devin.ai/desktop/changelog.md (`windsurf.com/changelog` 308s to docs.devin.ai and the rendered page is client-side, so the `.md` mirror is the only fetchable form) · https://docs.devin.ai/cli/changelog/stable.md (the **CLI** release stream, read for the first time on 2026-09-11, #737: four of six windsurf outputs, `.devin/agents/`, `.devin/hooks.v1.json`, `.devin/mcp_config.json`, and `AGENTS.md`, are Devin CLI surfaces, and the Desktop changelog above never covers them)
- watch: the adapter writes `.devin/rules/`; confirm that is still the read path after the Devin rebrand, and whether MCP config became project-scoped. `.agents/skills/` is a documented cross-agent-compatibility path behind Devin Desktop's own `.windsurf/skills/` (target-audit 2026-08-08, #563); re-check if that framing ever flips. Resolved (target-audit 2026-09-18, #863): `.windsurfignore` is not a legacy name at all. `/desktop/context-awareness/windsurf-ignore` calls only `.codeiumignore` legacy and gives the other two different jobs: `.devinignore` configures what Devin Desktop Indexing skips, and "The agent additionally respects `.windsurfignore` files when accessing files". One Ignore spec now writes both, and `outputs.windsurf.ignore-file` moves the indexing file only. Watch that sentence for the agent file being folded into `.devinignore`. Same audit, still open: `/cli/subagents` lists `.agents/agents/` under "Also supported" for project subagents, both `<name>.md` and `<name>/AGENT.md` (with `agent.md` accepted), and "a `name:` in the frontmatter overrides the identifier derived from the path". That is the tree antigravity, goose, and openhands write, so with windsurf enabled Devin sees two profiles per agent and only the `.devin/agents/` copy carries `allowed-tools`. A shared file is blocked on `model`, a model ID for Devin and the `inherit`/`flash`/`pro` tier enum for antigravity; documented as a cross-target hazard in `targets/_index.md` pending a design. The doc site also covers a separate Devin CLI / Devin Local Agent product tree (`/desktop/devin-local`, `/cli/extensibility/rules`, `/cli/extensibility/skills/overview`, `/cli/extensibility/mcp/configuration`), uncited here until now (confirmed 2026-08-09, #590); it documents a project-scoped `.devin/mcp_config.json`, now emitted (#587), plus `.devin/hooks.v1.json` (eight events, unwrapped, `type` accepts `prompt` as well as `command`), now emitted too (#629, re-confirmed live 2026-09-10: the lifecycle-hooks page's own tool-names table is lowercase snake_case, `exec`/`edit`/`read`/..., so a Claude-style matcher surfaces a coverage note the same way openhands' and antigravity's do). That shape had been targets.md's own hooks-decline reason for this target (#707), replacing a Cascade citation the removal below made stale; the decline reason itself is gone now that the shape emits. Confirmed 2026-09-09 (#707): the changelog's v3.9.19 entry ("September 8, 2026") reads "**Cascade** has been removed. Devin Local is now the only agent available in Devin Desktop", and `/desktop/devin-local`'s Limitations section reads "Workflows are not available with the Devin Local agent. Migrate your workflows to skills with the Devin: Open Cascade Migration Wizard command". `outputs.windsurf.workflows-dir` stopped emitting and only warns now; `/desktop/devin-local` and the whole `/desktop/cascade/*` tree still describe Cascade as present, so date the changelog entry as the authority and expect those feature pages to keep lagging it for a while. `.devin/agents/<name>.md` subagents are now emitted (#638): the frontmatter is `name`/`description`/`model`/`allowed-tools`/`max-nesting`, and the tool vocabulary is the closed five-name set on `/cli/reference/permissions` ("**Available tool names:** `read`, `edit`, `grep`, `glob`, `exec`") plus `mcp__<server>__<tool>`. Watch that list for a sixth name, since anything outside it drops today. The vendor calls custom subagents "**experimental**"; watch for the format to change. `AGENTS.md` is now emitted too (#645, "Devin CLI reads this file automatically", table row "Recommended"). Watch the rules-discovery mechanism specifically: the page describes `.devin/rules/` directories being found at each level between the workspace root and the cwd, and never says subdirectories *inside* `.devin/rules/` are read, which is where this adapter writes a scoped rule (target-audit 2026-08-27). A Devin CLI run showing a rule at `.devin/rules/x/y.md` absent from the loaded-rules list would settle it.
- verified 2026-09-18: [config file](https://docs.devin.ai/cli/reference/configuration/config-file.md) and [permissions](https://docs.devin.ai/cli/reference/permissions.md) confirm a committed `.devin/config.json` taking `permissions` with `allow`/`deny`/`ask`, now emitted (#856). "Only `permissions`, `read_config_from`, and `hooks` are available in project configs", and the `agent` block is user only, so a portable `model` stays out with a coverage note. Watch the rule vocabulary on the permissions page: the four scopes are `Read`, `Write`, `Exec`, `Fetch`, the five bare tool names are `read`/`edit`/`grep`/`glob`/`exec`, and `Exec` prefix-matches with no exact-command form, so an exact `Bash(cmd)` always widens when translated. Corrected 2026-09-19 (#887): which way that cuts depends on the list, and the blanket drop was wrong for one of them. The CLI changelog's v3000.10.31 entry (2026-09-16) reads "A command deny such as `Exec(rm)` blocks the command even when a broader `ask` or `allow` rule covers the whole tool", so widening a **deny** is strictly more restrictive and now emits; `allow` and `ask` still drop, since widening those approves commands nobody wrote. Watch for an exact-command form appearing on `Exec`, which would retire the widening entirely.

## continue

- status: **wound down.** continue.dev now titles itself "Continue (acquired by Cursor)" and its body reads "Continue has joined Cursor"; `continuedev/continue`'s newest `main` commit is 5522c6f dated 2026-07-21 (re-checked 2026-09-20). **Do not bound this target with `pushed_at`**: it read 2026-09-20 on that date, two months newer than `main`, because the field tracks any branch including dependabot pushes. Read `repos/continuedev/continue/commits/main` instead. The docs below still resolve and the adapter still emits, but treat the `watch:` re-read below as low priority until the repo moves again: a dormant vendor cannot drift.
- docs: https://docs.continue.dev/customize/deep-dives/rules · /customize/deep-dives/mcp · /reference (all client-rendered with no `.md` mirror and no `llms.txt`; the working source is `continuedev/continue` at `docs/customize/deep-dives/*.mdx`)
- changelog: https://github.com/continuedev/continue/releases
- watch: rule frontmatter is emitted since #639 (`name`, `globs`, `alwaysApply`, `description`, plus `regex` via `x-continue`); the field list lives in `docs/customize/deep-dives/rules.mdx`, so re-read it each audit for a sixth key. Also the `/hub/` namespace is gone from the doc source entirely (target-audit 2026-08-08, #563; confirmed by listing `continuedev/continue`'s `docs/` tree), so `.continue/rules/` vs any hub-successor surface if one reappears, the `.continue/mcpServers/*.yaml` schema (`packages/config-yaml/src/schemas/mcp/index.ts`, the union whose `type` literals and `requestOptions` nesting the emitter got wrong in target-audit 2026-09-11, #726 and #730; the prose page names transports but not field placement, so read the zod file), and whether `outputs.continue.assistants-dir` ever gains a vendor-confirmed native discovery path (today: an opt-in export whose file shape matches `promptSchema`/`configYamlSchema` in `continuedev/continue`'s `packages/config-yaml/src/schemas/index.ts` and `/reference`'s `name`/`version`/`schema: v1`, but no doc says Continue scans a project directory for it; see `docs/site/content/docs/targets/continue.md`).
- verified 2026-09-12: [MCP reference](https://docs.continue.dev/reference#mcpservers) and [schema](https://raw.githubusercontent.com/continuedev/continue/main/packages/config-yaml/src/schemas/mcp/index.ts) define stdio cwd, connectionTimeout, and remote requestOptions, now preserved through import and sync (#763). The [JSON loader](https://raw.githubusercontent.com/continuedev/continue/main/core/context/mcp/json/loadJsonMcpConfigs.ts) reads JSONC .json files as a root mcpServers map or a bare server; both import (#764). YAML block files remain exactly one server.

## amp

- docs: https://ampcode.com/llms.txt (the index; it lists every docs page and each link serves raw markdown) · https://ampcode.com/docs/customize/agents-md · /docs/customize/skills · /docs/customize/global-plugins-and-skills · /docs/customize/mcp · /docs/customize/plugins · /docs/cli/settings · /docs/the-dial · /docs/orbs/customizing · /docs/orbs/portals
- changelog: https://ampcode.com/chronicle (`ampcode.com/news` 307s here; individual posts keep `/news/<slug>`)
- key list: https://ampcode.com/cli-settings.schema.json is the authoritative `.amp/settings.json` key surface, a draft-07 JSON Schema titled "Amp CLI settings" and served straight from the vendor (200 on 2026-09-20). Diff it rather than re-reading `/docs/cli/settings` prose, which enumerates a subset.
- **no tool-name enumeration exists.** `https://ampcode.com/docs/tools` publishes no list of tool names (checked 2026-09-20); it describes the Librarian and the tool surface in prose only. That absence is the blocker on any portable permission mapping for amp, since a deny or allow rule has nothing to name. Watch for a tool table appearing there.
- watch: `.agents/skills/` (shared tree with codex/zed/crush), `amp.mcpServers` key in `.amp/settings.json`, and the orb lifecycle files `.agents/setup`, `.agents/resume`, and `.amp/services.yaml`. Environment emission now maps `install` to executable `.agents/setup` and `terminals` to supervised `.amp/services.yaml` (#637). `.agents/resume` stays unmapped because it runs after activation and every wake with thread credentials, which has no equivalent in the current Environment fields; watch for a portable resume or re-authentication intent rather than routing `SessionStart` blindly. `.agents/commands/` is **retired**: Amp removed custom commands on 2026-01-29, the migration steps end with "Delete the original command file", and a re-sweep of every page in `llms.txt` finds no file-based command surface. Nothing emits there now, for Agent specs either (target-audit 2026-09-11, #727); agents reach amp through `outputs.amp.rules-file` or not at all. Re-file only on a vendor page that names a file-based agent or command path, not on the plugin API, which is TypeScript. `/docs/the-dial` (new, 2026-09-19) says the same thing from the other side: its "Custom Modes" section routes custom agent modes through `/docs/customize/plugins#define-a-custom-agent-mode`, and the dial itself is configured under Settings, user and workspace tier. `/docs/customize/global-plugins-and-skills` is the other page added that day; it covers publishing extensions across machines, no project file. **`ampcode.com/manual` is dead and its 200 is a lie**: it returns 25 KB of SvelteKit shell with none of the manual text, so every claim that cited it needs a new source (target-audit 2026-08-27). `.agents/checks/` (the reviews surface a prior audit filed as a gap) is **gone**: a grep of every page in `llms.txt` returns zero hits for `checks/` or `severity-default` (re-confirmed 2026-09-11), and `/docs/customize/checks` 404s. Do not file a reviews emitter on the old evidence. `includeTools` is documented under "Common fields" for skill MCP servers but never named inside an `amp.mcpServers` example. The MCP page says servers "use the same configuration fields as MCP servers in skills", so the clause implies it and the enumeration does not; `x-amp` now carries it and any future field (#634), which is right either way. A vendor example showing `includeTools` inside an `amp.mcpServers` block would settle whether it earns a top-level mapping.

## zed

- docs: https://zed.dev/docs/ai/skills · /docs/ai/mcp · /docs/tasks · /docs/ai/instructions
- changelog: https://zed.dev/releases
- watch: rules library was retired in 1.4.2; `context_servers` (not `mcpServers`) key; whether a lifecycle-hook surface appeared. **Read `crates/settings_content/src/project.rs`, not `/docs/ai/mcp`, for the per-server field set.** The doc page names no per-server toggle at all (its only `enabled`-family key, `enable_all_context_servers`, is an agent-profile key), while every `ContextServerSettingsContent` variant in that Rust file carries `enabled: bool` defaulting to true, plus `timeout` and `oauth` on `Http` and `remote` on `Stdio`/`Extension`. `enabled` now emits from a spec's `disabled: true`; the other three stay reachable through `x-zed` (#641). Two red herrings that nearly sank that finding, worth not re-litigating: `configuring-zed.md` claims "Project settings are limited to editor behavior and language tooling options", and `context_servers` is absent from the all-settings reference. Both are wrong about `context_servers`, which is a field of `ProjectSettingsContent`. `/docs/ai/instructions` holds the project-instruction lookup order, nine paths, first match wins with no merge: `.rules`, `.cursorrules`, `.windsurfrules`, `.clinerules`, `.github/copilot-instructions.md`, `AGENT.md`, `AGENTS.md`, `CLAUDE.md`, `GEMINI.md`. sync writes zed's entry-point to `.rules` (rank 1) because copilot's pointer-only file at rank 5 outranked `AGENTS.md` at rank 7 and left Zed with no rules at all (#624). Zed calls `.rules` a compatibility instruction file and `AGENTS.md` its primary one, so **watch for `.rules` leaving that list**: it moves zed's entry-point back to `AGENTS.md`, and only then. The `x-zed` Task passthrough is a generic merge (`MergeCustomTargetMeta` copies any key), not an enumerated field list: `/docs/tasks` has grown fields since this adapter's own doc comment last enumerated one (`hooks` with `create_worktree`, `show_summary`, `show_command`, confirmed 2026-08-08, #563), which is exactly why an enumerated list here goes stale.
- verified 2026-09-20: `context_server_timeout` is a new user-managed key sitting beside `context_servers` in `ProjectSettingsContent` (`crates/settings_content/src/project.rs`), a global default in seconds that "Can be overridden per-server in context_servers configuration". The adapter's merge already preserves it, because it only replaces the `context_servers` map. Recorded so a future audit does not read it as drift.
- verified 2026-09-12: [Skill names](https://zed.dev/docs/ai/skills) require 1-64 lowercase alphanumeric characters with single hyphen separators; sync now validates the Zed constraint (#766). Native .agents/skills folders and assets import (#765).

## warp

- docs: https://docs.warp.dev/terminal/entry/yaml-workflows · https://docs.warp.dev/agents/capabilities/skills · https://docs.warp.dev/agents/capabilities/mcp
- changelog: https://docs.warp.dev/changelog/ (the `/getting-started/changelog` path 308s here). Fetch `https://docs.warp.dev/changelog/2026.md` for entry text: it is the per-year markdown mirror and the only form that yields entries in one call, since `/changelog.md` is a 518-byte year index.
- watch: whether Warp gained a native rules-dir surface (today: AGENTS.md + `.agents/skills/` + workflows + `.warp/.mcp.json`; skills confirmed #557). The 2026-08-07 changelog added a `WARP_SKILL_DIRS` env var (not `SKILLS_DIRS`, confirmed 2026-09-03, #663) for indexing extra skill directories beyond the ten documented defaults (`.agents/skills/` recommended, `.warp/skills/`, `.claude/skills/`, `.codex/skills/`, `.cursor/skills/`, `.gemini/skills/`, `.copilot/skills/`, `.factory/skills/`, `.github/skills/`, `.opencode/skills/`, confirmed 2026-08-09, #590), scoped to Cloud agents indexing skills outside the repo, not a general extension of the ten; re-check whether that changes which one this adapter should prefer. `.opencode/skills/` is OpenCode's own default, so Warp picks it up with no extra write. The MCP doc URL used to be `/knowledge-and-collaboration/mcp`; it now 308s to `/agents/capabilities/mcp` (confirmed 2026-08-11), so re-check for another move on the next pass. That page confirmed `working_directory` on the CLI Server (Command) table (the vendor's own name for the cross-tool `cwd` field, #606) alongside `command`/`args`/`env`, and confirmed the Streamable HTTP or SSE Server (URL) table has only `url`/`headers` (no discriminant, #592). It documents no `description`, `disabled`, or `roots` field anywhere, and this adapter stopped emitting all three in #641: they were inherited from the shared builder it used before #606 and carried through unexamined. `disabled` raises a coverage note; `description` and `roots` go in through `x-warp` if a user wants them. Re-check whether either table grows a home for any of the three.
- verified 2026-09-12: Native .agents/skills folders and bundled assets now import, matching the emitted default (#765).

## opencode

- docs: https://opencode.ai/docs/agents/ · /docs/skills/ · /docs/mcp-servers/ · /docs/commands/ · /docs/rules/ · /docs/policies/ (the experimental `experimental.policies` array in `opencode.json`, which gates which providers and resources OpenCode may use; separate from permissions, which gate what tools may do) · /docs/plugins/ (`.opencode/plugins/*.ts`, the hook surface) · /docs/permissions/ (the top-level `permission` key in `opencode.json`, the page that settles what the portable lists map onto)
- trap (2026-09-20): `/docs/permissions/` describes `webfetch` as "fetching a URL (matches the URL)" and `websearch` as "web search (matches the query)", which reads as though the pattern-object exclusion at `internal/adapters/opencode/permission.go:47` is wrong. It is not. `/docs/agents/` settles it: "`read`, `edit`, `glob`, `grep`, `list`, `bash`, `task`, `external_directory`, `lsp`, and `skill` accept either a shorthand action ... or an object of glob/pattern to action for fine-grained control. The remaining keys accept the shorthand action only." `$defs.PermissionConfig` types those two as `PermissionActionConfig`, agreeing.
- changelog: https://github.com/anomalyco/opencode/releases (the repo moved from `sst`; the redirect still works, and the default branch is `dev`)
- watch: `agents/` (plural) dir, which foreign skill trees it also scans, `opencode.json` `mcp` block shape. The rules lookup is an upward walk for files named exactly `AGENTS.md`, confirmed in the vendor's own source at `packages/core/src/instruction-context.ts` on branch `dev` (`fs.up({ targets: ["AGENTS.md"] })`); no doc or code path names `.opencode/AGENTS.md`, which is where this repo wrote opencode's entry point until #623 moved it to the root `AGENTS.md` (target-audit 2026-08-27). Re-check that the upward walk still targets `AGENTS.md` only, and that the global `~/.config/opencode/AGENTS.md` slot stays out of project scope. Agent `tools` is now vendor-deprecated in favour of `permission`, which our frontmatter filter already emits, so that choice is confirmed rather than a gap. The MCP tables carry `cwd` (local only) and `timeout` (both, milliseconds, default 5000), both emitting since #641; `oauth` on a remote entry stays reachable through `x-opencode`. Hooks are codegen, not a config key: `/docs/plugins/` documents `.opencode/plugins/` as a project-level directory whose JS and TS files load at startup, and the vendor's own examples fix the two shapes this adapter emits since #892, a direct `"tool.execute.before"` key taking `(input, output)` and a single `event` hook switching on `event.type`. Watch the page's own "Events" section for new entries, and watch whether `experimental.session.compacting` loses its prefix; both it and `shell.env` are declined with a coverage note because they exist to rewrite `output.env`/`output.context`/`output.prompt` rather than run a command. Tool names in `input.tool` are lowercase in every vendor example (`bash`, `read`), so a Claude-cased matcher gets the same coverage note crush and openhands give their own vocabularies; re-check if the vendor ever publishes the full tool list.
 **Permissions are a confirmed gap, not a missing key** (target-audit 2026-09-19, #922): `/docs/permissions/` documents a top-level `permission` key in `opencode.json`, the same project file this adapter already merges `model` and `mcp` into, and its actions are `allow`, `ask`, `deny`, the exact portable list names. The adapter writes none of it and raises no note. Do not re-derive this from `/docs/policies/`, which is a different feature: policies gate providers and resources, permissions gate tools. Three things make it more than a port: `Edit` and `Write` both collapse onto opencode's single `edit`, the pattern grammar is bare glob (`git *`) rather than the portable colon convention (`Bash(git:*)`), and matching is **last-match-wins** with the catch-all first, so the three unordered lists must be flattened into one ordered array. **Corrected 2026-09-20 (#947): there IS an MCP route, and the sentence that used to sit here is what caused us to ship a false coverage note.** `/docs/agents/` states: "Permission keys are matched as wildcard patterns against the underlying tool name, so the same syntax works for built-ins, custom tools, and MCP tools", with `"mymcp_*": "deny"` and `"mymcp_search": "ask"` as its own examples. So `mcp__<server>__<tool>` maps to `<server>_<tool>`, exactly as kilo does, and the server name passes through verbatim including hyphens (`my-mcp` yields `my-mcp_search`). Only a **whole-server** rule stays noted, because this project has no portable bare-server spelling: `spec.SplitPermissionRule`'s MCP form requires both halves. Two method notes. The schema confirms it, but only if you resolve the root `$ref` to `#/$defs/Config` first and read `$defs.PermissionConfig.additionalProperties`; reading the root and seeing a `$ref` is what previously produced "opencode has no permission key at all". And read `/docs/agents/` as well as `/docs/permissions/`: the former carries permission detail the latter omits.
## antigravity

- docs: https://antigravity.google/docs/rules-workflows?tab=ide · /docs/skills?tab=ide · /docs/mcp?tab=ide · /docs/hooks?tab=ide · /docs/plugins · /docs/slash-commands · /docs/ide/workflows (still on the old path, and deprecated: "Workflows are being deprecated in favor of **Agent Skills** by November 1, 2026") · https://antigravity.google/docs/subagents (the `/docs/ide/subagents` variant 404s; this is the real path) · https://codelabs.developers.google.com/getting-started-with-antigravity-skills
- changelog: https://antigravity.google/changelog (live, updates frequently; splits into four product tabs, Hub/2.0, IDE, CLI, and SDK, each on its own version track, confirmed 2026-08-09, #590; filter to the IDE tab, since that is what this adapter targets)
- watch: MCP schema fields beyond the confirmed set (stdio's `command`/`args`/`env`/`cwd`, remote's `serverUrl`/`headers`, both transports' `disabled`, confirmed #556). `description`, `roots`, and a `type` discriminant remain unconfirmed and are omitted from the adapter. The `/docs/ide/*` split is over: as of 2026-09-18 `/docs/ide/rules`, `/docs/ide/skills`, `/docs/ide/mcp`, and `/docs/ide/hooks` each serve a 400-byte `<meta http-equiv="refresh">` stub pointing at `/docs/rules-workflows?tab=ide`, `/docs/skills?tab=ide`, `/docs/mcp?tab=ide`, and `/docs/hooks?tab=ide`, and their `.md` mirrors 404 while the new ones return 200 (#865). One page now carries a tab per surface (Antigravity 2.0, CLI, IDE), so `?tab=ide` selects a section rather than a separate document. `/docs/ide/workflows` did not move. `https://antigravity.google/llms.txt` is the cheapest confirmation of the canonical set. Rule activation is the open question: the vendor names four modes (Manual, Always on, Model decision, Glob pattern) in prose about the Customizations panel and no page on the host names a frontmatter key, a file format, or an example, so this adapter writes a bare file and notes the drop (#865). Capture IDE-generated rule files for each mode before modelling it; Windsurf's `trigger:`/`globs:` spelling is a lineage guess, not evidence. No page names an AGENTS.md-family project instruction file either: `AGENTS.md` appears in zero of the 18 docs pages, and `/docs/rules-workflows` names only `.agents/rules/` for workspace rules and `~/.gemini/GEMINI.md` for global ones. So `.agent/AGENTS.md` is an agnostic-ai convention keeping the entry point clear of codex, amp, and warp at the project root, not a documented read path (#865). Hooks have a documented schema (`/docs/hooks?tab=ide`: `.agents/hooks.json`, five events, `PreToolUse`/`PostToolUse`/`PreInvocation`/`PostInvocation`/`Stop`), and **the IDE-executes-them question is settled**: the hook payload's `transcriptPath` resolves under `~/.gemini/antigravity-ide`, the IDE's own app-data directory, so the IDE is the process running them (target-audit 2026-08-27). Commands support remains fully unconfirmed in the public-preview docs. Appending `.md` to any antigravity **docs** path serves a clean markdown mirror past the client-side rendering; the changelog is not a docs path, `changelog.md` 404s, and the page itself is server-rendered HTML that plain curl plus a tag-strip reads fine (2026-09-11, #737). Pass `--compressed` anyway: one audit got raw brotli back from an edge that ignored the missing `Accept-Encoding`. Subagents emit to the vendor-supported nested `.agents/agents/<name>/agent.md` form (#717); Goose skips nested directories, so its flat shared profiles do not collide. Their `tools` list is Antigravity's own vocabulary (`view_file`, `replace_file_content`, `grep_search`, `run_command`, ...) with **zero** overlap with Claude-style names, and the page warns that an unmapped name can hang the subagent, so this adapter never writes a generic `tools` and never should: `x-antigravity.tools` is the only channel. `model` there is the tier enum `inherit`/`flash`/`pro`, not a model ID, so watch for a fourth tier. Every other documented key (`mainAgent`, `subagent`, `commandExecutionPolicy`, `mcpServers`, `skills`/`plugins`) rides `x-antigravity`; watch for one worth a first-class mapping. The Antigravity 2.0 / Hub track shipped `inheritCustomizations` (2.9.1) and a `rules:` frontmatter key plus `skills.json`/`agents.json`/`rules.json` subdirectory discovery (2.11.0) in August 2026. **Narrowed 2026-09-20**, because part of that note has since landed: `@`-mention file includes are now documented, `/docs/rules-workflows` carrying an "@ mentions" section for referencing other files from a rules file by relative or absolute path. The other three are still absent from the docs. `inheritCustomizations` returns zero hits across `/docs/rules-workflows`, `/docs/skills`, `/docs/mcp`, `/docs/hooks`, `/docs/plugins`, `/docs/slash-commands` and `/docs/subagents`, and appears only in changelog prose; so do the three discovery JSON files. None is IDE-attributed. Keep watching for the docs to catch up on those three rather than modelling them. Two more from target-audit 2026-09-19 (#896): the IDE tab's skill-locations table rows the global scope as `~/.gemini/config/skills/<skill-folder>/` and calls `~/.gemini/antigravity/skills/` legacy, while the Antigravity 2.0 tab names the config path with no legacy alternative at all, so `sync --global` moved off the legacy tree; and `/docs/rules-workflows?tab=ide` states "Rules files are limited to 12,000 characters each", now a coverage note, with the vendor silent on whether it truncates or rejects. **Open question, `/learn` and `.antigravity/rules.md`** (target-audit 2026-09-19): `/docs/slash-commands` describes `/learn` as "distilling them into persistent project Rules (`.antigravity/rules.md`) or reusable Agent Skills (`SKILL.md`)". That path appears exactly once on the whole host, in prose about a slash command. Every other page contradicts it: `/docs/rules-workflows?tab=ide` reads "Antigravity defaults to `.agents/rules`, but still maintains backward compatibility for `.agent/rules`", and its IDE tab reads "Workspace rules: stored in `.agents/rules/` within your open project." Impact today is zero, and the adapter keeps `defaultRulesDir = ".agents/rules"`. Most likely stale copy. **What settles it**: run `/learn` in the IDE and check which path the written file lands on. Recorded here so the next audit reads this line instead of re-deriving it, and does not file it on the prose alone. Appending `.md` to any antigravity **docs** path serves a clean markdown mirror past the client-side rendering; the changelog is not a docs path, `changelog.md` 404s, and the page itself is server-rendered HTML that plain curl plus a tag-strip reads fine (2026-09-11, #737). Pass `--compressed` anyway: one audit got raw brotli back from an edge that ignored the missing `Accept-Encoding`. Subagents emit to the vendor-supported nested `.agents/agents/<name>/agent.md` form (#717); Goose skips nested directories, so its flat shared profiles do not collide. Their `tools` list is Antigravity's own vocabulary (`view_file`, `replace_file_content`, `grep_search`, `run_command`, ...) with **zero** overlap with Claude-style names, and the page warns that an unmapped name can hang the subagent, so this adapter never writes a generic `tools` and never should: `x-antigravity.tools` is the only channel. `model` there is the tier enum `inherit`/`flash`/`pro`, not a model ID, so watch for a fourth tier. Every other documented key (`mainAgent`, `subagent`, `commandExecutionPolicy`, `mcpServers`, `skills`/`plugins`) rides `x-antigravity`; watch for one worth a first-class mapping. The Antigravity 2.0 / Hub track shipped `inheritCustomizations` (2.9.1) and a `rules:` frontmatter key plus `skills.json`/`agents.json`/`rules.json` subdirectory discovery (2.11.0) in August 2026. **Narrowed 2026-09-20**, because part of that note has since landed: `@`-mention file includes are now documented, `/docs/rules-workflows` carrying an "@ mentions" section for referencing other files from a rules file by relative or absolute path. The other three are still absent from the docs. `inheritCustomizations` returns zero hits across `/docs/rules-workflows`, `/docs/skills`, `/docs/mcp`, `/docs/hooks`, `/docs/plugins`, `/docs/slash-commands` and `/docs/subagents`, and appears only in changelog prose; so do the three discovery JSON files. None is IDE-attributed. Keep watching for the docs to catch up on those three rather than modelling them.
- verified 2026-09-12: Native .agents/skills folders and bundled assets now import, with .agent/skills as fallback only when the preferred directory is absent (#765).

## junie

- docs: https://junie.jetbrains.com/docs/get-started-with-junie.html · /docs/agent-skills.html · /docs/junie-ide-plugin.html (guidelines lookup order) · /docs/guidelines-and-memory.html (CLI guidelines lookup order, confirms the same order) · /docs/junie-cli-subagents.html (CLI-only native subagents, `.junie/agents/` or `.agents/`, live since 2026-03-10) · /docs/custom-slash-commands.html (CLI-only native slash commands, `.junie/commands/`, live since 2026-04-13) · /docs/junie-cli-hooks.html · /docs/junie-cli-mcp-configuration.html · /docs/junie-cli-extensions.html · /docs/junie-cli-configuration.html · /docs/junie-review-agent.html (the `/review` slash command and `junie --review`; a read-only subagent that "reads project guidelines and any code-review-related agent skills", so our skills and guidelines reach it) · https://junie.jetbrains.com/docs/custom-llm-models.html (project profiles under `.junie/models/*.json`) · /docs/environment-variables.html (the fullest statement of the guidelines lookup) · /docs/junie-ide-plugin.html again for `.aiignore` (the only page that names it) · /docs/action-allowlist-junie-cli.html (the allowlist, documented at `~/.junie/` only; reachable from `HelpTOC.json`, not from the visible nav) · /docs/junie-cli-demo.html (listed in `HelpTOC.json`, uncited until target-audit 2026-09-20)
- changelog: https://plugins.jetbrains.com/plugin/26104-junie-the-ai-coding-agent-by-jetbrains/versions · https://junie.jetbrains.com/blog/
- watch: the guidelines lookup order is strict precedence, first match wins, not a merge (#552): `.junie/AGENTS.md`, then root `AGENTS.md`, then legacy `.junie/guidelines.md` / `.junie/guidelines/`. `sync` always writes `.junie/AGENTS.md`, so re-check whether that still holds on every audit: a vendor change to step 1 would make everything below it live again. Also watch `.junie/mcp/mcp.json` schema. Three consecutive audits (#552, #590, and whatever landed between) missed junie-cli-subagents.html and custom-slash-commands.html even though both pages predate junie's own adapter issue (#474, 2026-07-19), because each audit checked "does what we emit still land" rather than "did the vendor grow a surface we never modelled" (#604, #605). Re-run that second question explicitly for junie every audit: check `get-started-with-junie.html`'s own nav/sidebar for any page not already listed above, since that is how both were found. The subagent frontmatter table (`name`, `description`, `tools`, `disallowedTools`, `mcpServers`, `model`, `reasoningLevel`, `maxTurns`, `skills`, `allowPromptArgument`) and the "Configure subagent usage" auto model-selection toggle (Early Access, `/settings` only, not a file format) are two different things; only the latter is caveated. The full page list comes from `https://junie.jetbrains.com/docs/HelpTOC.json` in one call, which is how the uncited pages above were found; run that read every audit. It is the fastest route to all 63 pages, and on 2026-09-11 it surfaced `junie-plugin-project-settings.html`, which led to `.aiignore` (#728). Two updates from 2026-08-27: the lookup order's step 2 now reads "`AGENTS.md` file in the project root, **combined with `.junie/playbook.md` and every `.junie/rules/*.md` file, if present**" on both the CLI and IDE pages, so `.junie/rules/` is shadowed by our `.junie/AGENTS.md` rather than unread as this adapter's doc claims; and the subagent table gained `permissionMode` plus an `effort` alias for `reasoningLevel`. Both are docs-only, since `DocumentStyled` passes every key through. Junie hooks stay correctly declined on two independent grounds, Early Access and "Project-local hooks from `<project-root>/.junie/config.json` are ignored by default for safety". `.aiignore` (project root, "the same syntax and pattern format as the `.gitignore` file") is emitted since #728 and is documented on `junie-ide-plugin.html` alone: no CLI page names it, so an import-side reader must tolerate its absence, and a CLI page appearing is the change to watch. Watch the enforcement wording too, which is softer than every other ignore target's: Junie "will ask for explicit approval before viewing or editing" a listed file, only contents are protected (names stay visible), and Brave Mode or an allowlisted command bypasses the prompt.
 **`agent-skills.html` now names a third project root** (2026-09-20): "Junie CLI also loads skills from: `.agents/skills/` directories: `<projectRoot>/.agents/skills/` (in a trusted project) and `~/.agents/skills/`", alongside `.junie/skills/`. The same page says the default project and user locations, the `.agents/skills/` pair included, can be disabled with `--skill-default-locations false`. So junie joins the shared `.agents/skills/` tree ten other targets already write; watch that list for a fourth root.
 **No project-tier permission surface, settled** (target-audit 2026-09-19, #917): `/docs/action-allowlist-junie-cli.html` documents the only rule-based approvals file at `~/.junie/allowlist.json` alone, with no project variant and no relocation flag, unlike `--config-location` for the config file. The project tier is real for other fields, which is what makes this a checked absence: `/docs/junie-cli-configuration.html` names both `~/.junie/config.json` and `<project-root>/.junie/config.json`, then tables that file's fields, and none is an allow, deny or ask list. `brave` is one boolean that skips every prompt, not a rule list. The IDE's Action Allowlist is a Settings panel, not a file. The adapter now raises a coverage note. Watch for an allow/deny/ask key appearing in the project config field table, which would retire it.
## kiro

- docs: https://kiro.dev/docs/steering/ · /docs/mcp/ · /docs/mcp/configuration/ · /docs/hooks/ · /docs/hooks/types/ · /docs/hooks/actions/ · /docs/skills/ · /docs/custom-agents/ · /docs/custom-agents/configuration-reference/ · /docs/tools/ · https://kiro.dev/docs/kiroignore/ · https://kiro.dev/docs/cli/v3/hooks-migration/ (the vendor's own current link for the 2.x-to-3.0 hook migration page; the `/docs/cli/v3/hooks/` form recorded below still resolves to the same page, both 200 through the proxy on 2026-09-20)
- changelog: https://kiro.dev/changelog/ (the slashless form 301s here)
- watch: steering `inclusion:` values (`always` / `fileMatch`); the agent `tools` field's category vocabulary, confirmed 2026-08-08 (`read`/`write`/`shell`/`web`/`subagent`/`knowledge`/`todo_list`, plus `@server_name`/`@mcp`/`@builtin`/`*`). Re-check whenever Kiro adds, renames, or splits a category, since `kiroToolCategory` in kiro.go maps agnostic-ai's Read/Write/Edit/Bash/Grep/Glob/WebFetch/WebSearch onto it. The hook trigger vocabulary is settled: `/docs/hooks/types.md` (dateModified 2026-09-02) tables an "Availability by surface" list of exactly the 11 v1 triggers `hookEventsByTarget["kiro"]` already carries, plus a twelfth, Legacy Manual Hook, which the same page rules out ("Legacy manual Hooks don't have a v1 trigger equivalent, and you can't create a new Manual-trigger Hook"), so its absence from the map is correct, not a gap. Take literal casing from `/docs/hooks/actions.md`, never from the title-case display names, since the mapping isn't mechanical ("Prompt Submit" is `UserPromptSubmit`, "Pre Task Execution" is `PreTaskExec`); that page confirms both verbatim ("Exit code 2: Block execution (`PreToolUse`, `UserPromptSubmit`, `PreTaskExec` only)" and "`AgentSpawn` hooks are never cached"). Re-check only if the triggers table itself gains a row. The old `/docs/cli/custom-agents/configuration-reference/` path now serves a client-side meta-refresh and JS redirect (HTTP 200, not a real 3xx) to the URL above; curl with `-D -` to inspect headers and body before concluding a vendor page is gone or undocumented, since WebFetch does not follow a meta-refresh and an earlier audit misread this exact stub as an unconfirmed vocabulary. The docs are Next.js server-rendered, so plain curl plus a tag-strip gives full text; WebFetch truncates the 280 KB pages. Three things confirmed 2026-08-27: the hook `version` field is the **string** `"v1"`, not the integer this adapter emits; `/docs/mcp/configuration/` documents `oauth`, `oauthScopes`, `autoApprove` and `disabledTools`, all four emitting since #634 behind `emit.WithKiroMCPExtras()` (`oauth` and `oauthScopes` on remote servers only, per the vendor's own two tables), and it also confirms `disabled` on both tables, which the shared builder already wrote; and `/docs/skills/` documents `.kiro/skills/` with the standard `SKILL.md` folder layout, which this adapter now writes natively (#642), sweeping a stale `.kiro/steering/skill-<name>.md` left by a pre-native sync. `/docs/hooks/` also documents `hooks[].description` and `hooks[].confirm`, unreachable before #642 since the emitted shape was a fixed Go struct with no route for either key, including through `x-kiro`; both now emit (description generically, confirm only via `x-kiro`). Also watch whether Kiro ever names a `type` discriminant on a remote MCP entry: its remote table does not list one and we emit `"type": "http"`. `/docs/kiroignore/` (dateModified 2026-08-21) documents `.kiroignore` in the project root with standard gitignore syntax, emitted since #728; its "Capability" table is the part that churns, since IDE enforcement is gated on the filename being present in the `kiroAgent.agentIgnoreFiles` setting and CLI V3 applies the file to search results only. Re-read that table before claiming the ignore file is enforced everywhere, and watch for Web or Mobile gaining a column.
- **kiro.dev blocks this network; use the reader proxy.** Every kiro.dev path returns HTTP 403 with a CloudFront body ("Request blocked") to curl and to WebFetch, on any user agent, and the Internet Archive holds no snapshot of the hook pages. `https://r.jina.ai/https://kiro.dev/<path>` fetches server-side and returns clean markdown, the same proxy this file records for cursor.com. Verified 2026-09-19. Do not record kiro as unauditable without trying it: the 2026-09-19 audit did, and the target went a full run unchecked for nothing.
- verified 2026-09-19 (#907) via the proxy: https://kiro.dev/docs/cli/v3/hooks/ is the 2.x-to-3.0 migration page and https://kiro.dev/docs/hooks/types/ is the trigger reference. **camelCase is the old 2.x format**, embedded in agent config (`agentSpawn`, `preToolUse`, `fileEdited`); 3.0 uses PascalCase `trigger` values in standalone `.kiro/hooks/*.json`, which is what this adapter already emits. The eleven triggers are `SessionStart`, `Stop`, `PreToolUse`, `PostToolUse`, `PreTaskExec`, `PostTaskExec`, `UserPromptSubmit`, `PostFileCreate`, `PostFileSave`, `PostFileDelete`, `Manual`. Documented renames: `agentSpawn` to `SessionStart`, `userPromptSubmit` to `UserPromptSubmit`, `preToolUse` to `PreToolUse`, `postToolUse` to `PostToolUse`, `fileEdited` to `PostFileSave`, `fileCreated` to `PostFileCreate`. All eleven URLs in this section were re-fetched successfully through the proxy on 2026-09-20; none has moved. **Tell CLI 3.0 from 2.x by schema keys, never by page dates.** A `trigger` key inside a `version: v1` envelope is the 3.0 standalone `.kiro/hooks/*.json` format, which is what this adapter writes. A `hook_event_name` key, or hook definitions sitting among agent-config keys, is 2.x. Page dates are not a tiebreak: the migration page and the trigger reference are both current and describe different formats on purpose, so a newer date on either proves nothing about which format is live. Two nuances worth not re-deriving. `AgentSpawn` WAS documented when #660 landed, quoted then from `/docs/hooks/actions/` ("AgentSpawn hooks are never cached"); that sentence is gone and the name now returns zero matches on `/docs/hooks/`, `/docs/hooks/types/` and `/docs/hooks/actions/`, so 3.0 dropped it rather than us having misread it. And `Manual` looks contradicted by `/docs/hooks/types/` ("Legacy manual Hooks don't have a v1 trigger equivalent, and you can't create a new Manual-trigger Hook"), but that section is about IDE 0.x manual hooks in the Agent Hooks panel, a different surface. The CLI page lists `Manual` twice as a current v1 trigger, and the v1 JSON file schema is what this adapter writes.
- verified 2026-09-12: [Hook actions](https://kiro.dev/docs/hooks/actions/) document zero timeout and native agent actions (#772). A valid x-kiro.action now works without command and replaces the whole fallback command list; invalid actions error. Omitted timeout still uses the vendor default.

## crush

- docs: https://github.com/charmbracelet/crush (README is the reference) · https://raw.githubusercontent.com/charmbracelet/crush/main/schema.json (the vendor's published JSON schema, and the only place the MCP property set appears closed) · https://github.com/charmbracelet/crush/blob/main/docs/hooks/README.md
- changelog: https://github.com/charmbracelet/crush/releases (the feed carries a rolling `nightly` tag whose timestamp bumps daily while its body stays static artifact-verification boilerplate; it reads like a daily ship and is not one, so compare against the newest real version tag, target-audit 2026-09-07)
- watch: `crush.json` `mcp` block, whether agents/commands surfaces landed, and `crushrc` / `.crushrc`, the Bash config format the vendor now prefers over JSON (docs/config/README.md's precedence table: `.crushrc` > `crushrc` > `$XDG_CONFIG_HOME/crush/crushrc`, ahead of legacy `.crush.json` / `crush.json` in the same directories; everything found merges, with `crushrc` overriding JSON in the same directory and Crush logging a warning when a folder has both). Tracked in #674. Read `schema.json` rather than the README for MCP: it is the only place the property set appears closed, and it sets `"additionalProperties": false` on `MCPConfig` — so a generic `x-crush` passthrough on MCP entries would let a typo produce a config Crush rejects outright. Prefer explicit field mapping there, unlike skill frontmatter where the generic merge is safe. `disabled`, `sessionless` (new in v0.91.2, 2026-08-26), `enabled_tools`, and `disabled_tools` now emit (#634, #641). Still unmapped: `timeout`, which schema.json documents as **seconds defaulting to 10** while the spec's own `timeout` is milliseconds on gemini, claude, opencode, and qoder. A blind top-level map would read `timeout: 5000` as 5000 seconds, so this needs a unit decision before it lands. Hooks are project-scoped in `crush.json` and support `PreToolUse` only; the matcher vocabulary is lowercase in the vendor's own example (`^bash$`). Now emitted too (#629, re-confirmed live 2026-09-10 against both `docs/hooks/README.md` — "Crush currently supports just one hook, PreToolUse, with plans to support the full gamut" — and `schema.json`'s `$defs.HookConfig`, which still lists no other event): `hooks.PreToolUse` merges into the same `crush.json` write as `mcp`, one `MergeJSONFile` call, not two. A Claude-style matcher (`Bash`, `Edit`, ...) surfaces a coverage note the same way openhands' and windsurf's own tool-name mismatches do. Watch for the vendor to add a second event, per its own "plans to support the full gamut" line.
- verified 2026-09-12: [Released v0.94.1 README](https://raw.githubusercontent.com/charmbracelet/crush/v0.94.1/README.md) documents .crushignore with gitignore syntax at root or in subdirectories. The adapter emits and imports the project-root file (#770); new ignore scope inheritance is excluded.

## trae

- docs: https://docs.trae.ai/ide/rules · https://docs.trae.ai/ide/model-context-protocol · https://docs.trae.ai/ide/add-mcp-servers · https://docs.trae.ai/ide/skills · https://docs.trae.ai/ide/subagents · https://docs.trae.ai/ide/slash-commands · https://docs.trae.ai/ide/hook-configuration-reference · https://docs.trae.ai/ide/automate-actions-with-hooks · https://docs.trae.ai/ide/ignore-files
- changelog: https://www.trae.ai/api/changelog (primary JSON feed, latest entry 2026-09-01) · https://docs.trae.ai/ide/changelog (documentation alternative, latest entry 2026-08-19; checked 2026-09-12)
- watch: `docs.trae.ai/ide/mcp`, the old URL for the MCP page, now 302s
  to a marketing page; model-context-protocol (the MCP overview) and
  add-mcp-servers (the config how-to) are the two live ones. Confirm
  both stay live and watch whether Trae ever adds a `type` discriminant
  or a `disabled` key: the current adapter (`.trae/mcp.json`)
  deliberately omits both because neither appears in the doc's two
  worked examples. Fetching: neither WebFetch nor a `.md` suffix works here
  (`ide/rules.md` returns 200 with 1.8 MB of SPA shell). The content is
  server-side inside `window._ROUTER_DATA` as a Quill delta: curl to disk,
  brace-match the object, then collect `ops[].insert` per `zoneId`, since
  table cells arrive as separate zones. Confirmed 2026-08-27: `/ide/subagents`
  documents `{project_folder}/.trae/agents/{my_agent}.md` with `name`,
  `description`, `model`, `tools`, `disallowedTools`, `mcpServers`, gated
  behind a Settings > Beta toggle whose default is unstated. That path is
  emitted since #638; watch whether the vendor ever states the toggle's
  default, and watch the "Available models" table, since a generic
  `model:` is dropped for lack of a value in it. `/ide/rules`
  documents `scene: git_message` and caps rule nesting at three levels;
  emitted since #635 via `x-trae.scene`, re-confirmed live 2026-09-10 by
  extracting the same Quill delta. `/ide/slash-commands` documents `.trae/commands` with a `Name`/`Description`
  table, which retires this repo's claim that the command shape was only
  reverse-engineered. Trae's **subagent** tool vocabulary is Claude-style
  and matches agnostic-ai's exactly, unlike kiro, factory, and
  antigravity. **Its hook vocabulary is not the same table**, and that is
  the trap to re-check every audit (#729): `/ide/hook-configuration-reference`
  tables `tool_name` as `Read`, `Write`, `Edit`, `Glob`, `Grep`, `LS`,
  `RunCommand`, `WebSearch`, `WebFetch`, `AskUserQuestion`, `Skill`, and
  `mcp__<serverName>__<toolName>`, where the terminal tool is
  `RunCommand`, not `Bash`, and both `LSP` and `TodoWrite` are absent. Never share one
  table between the two surfaces; a Claude-style matcher parses as a
  valid regex there and matches nothing, which is why hooks.go carries
  its own list and a coverage note. Also confirmed 2026-09-11:
  `/ide/ignore-files` documents `.trae/.ignore` ("TraeCode automatically
  creates the `.ignore` file in the `.trae/` folder"), effective only
  after a re-index, and `/ide/automate-actions-with-hooks` tables exactly
  six hook events plus the Settings > Hooks consent panel that enables
  the file. **Both pages were found by walking `window._ROUTER_DATA`'s
  nav tree**, which carries the full `{path, title}` list: neither the
  English sidebar nor the changelog named them, and three audits missed
  both. Walk that tree every audit rather than the rendered sidebar.
  Watch the hook schema's `version` (integer 1, "currently only 1 is
  supported") and `loop_limit` (Stop only). **Trae also reads Claude
  Code's hook file** (target-audit 2026-09-12, #756):
  `/ide/hook-configuration-reference` states "Additionally, TraeCode
  supports reading hook configurations from Claude Code", lists
  `$PROJECT_FOLDER/.claude/settings.json` and `.claude/settings.local.json`
  in its own Project Hook table, and adds "If both Claude Code hooks and
  TraeCode hooks are enabled at the same time, TraeCode will read all
  enabled hook configurations and execute them in combination." We write
  `.trae/hooks.json` (#729) and `.claude/settings.json` in any repo
  listing both targets, so a user who turns that on runs every hook
  twice. It is off by default: `/ide/automate-actions-with-hooks` says
  "Toggle the **Import Hook configuration in CLAUDE** switch on", behind
  the same security-warning panel as the project hook itself. Watch for
  that switch defaulting on, which turns this from upkeep into a finding.

## jules

- docs: https://jules.google/docs
- changelog: https://jules.google/docs/changelog/
- watch: today AGENTS.md only; any per-file surface is new.

## goose

- docs: https://github.com/aaif-goose/goose/blob/main/documentation/docs/guides/context-engineering/using-goosehints.md · .../using-skills.md · .../custom-agents.md · .../hooks.md · .../plugins.md (the Open Plugins package shape: manifest, component directories, and the project `.agents/plugins/<plugin-name>/` location) (the GitHub `documentation/docs/` tree is the working source; the rendered site at goose-docs.ai is not fetchable and its `llms.txt` lists no skills, agents, or hooks page)
- changelog: https://github.com/aaif-goose/goose/releases
- watch: `.goosehints` vs AGENTS.md precedence, whether recipes/extensions became project-scoped files. `goose-docs.ai/docs/guides/context-engineering/using-goosehints/` documents nested discovery: goose loads context files from the working directory up to the repo root, then discovers additional hint files in nested subdirectories as it reads or modifies files there, checking each for one of `CONTEXT_FILE_NAMES` (default `AGENTS.md`, `.goosehints`). This adapter now routes a scoped rule into a nested `<scope>/.goosehints` off that mechanism (#608); re-check `CONTEXT_FILE_NAMES` on future audits in case the vendor adds a third name or changes the default pair. `github.com/block/goose` 301s here (moved to the Agentic AI Foundation, 2026-04-07); the redirect still works, so this was a low-priority citation refresh, not a docs-moved finding (target-audit 2026-08-08, #563). Skills (`.agents/skills/`, "the recommended standard") are now declared (#632). Project hooks now emit as a complete Open Plugins package under `.agents/plugins/agnostic-ai/`, with the required manifest, all twelve events, regex matchers, seconds-based timeouts, and `x-goose.on_failure` for `PreToolUse` (#629). Project agents now emit to flat `.agents/agents/<name>.md` with `name`/`description`/`model` (#631); v1.50.0's loader skips nested directories, which lets Antigravity coexist at `<name>/agent.md` (#717). Slash commands stay correctly declined: they are user-tier only, under `slash_commands:` in `~/.config/goose/config.yaml`. `plugins.md` is the authority on the plugin package, not the hooks page: "A plugin can provide skills, hooks, or both" and "A plugin is a directory with a plugin manifest and optional component directories", so `plugin.json` now follows a skills-only bundle too (target-audit 2026-09-18, #862). Watch the component table for a third component beyond skills and hooks, and watch whether project plugins ever move off `.agents/plugins/`.
- verified 2026-09-12: [Released v1.50.0 CLI reference](https://raw.githubusercontent.com/aaif-goose/goose/v1.50.0/documentation/docs/guides/goose-cli-commands.md) and [shipping loader](https://raw.githubusercontent.com/aaif-goose/goose/v1.50.0/crates/goose/src/checks/mod.rs) confirm root .agents/REVIEW.md and <scope>/.agents/REVIEW.md (#771). The loader reads plain text (281-293), collects directories containing changed files and their ancestors (381-392), and creates distinct checks so root and scoped instructions compose (248-277). Its test at 726-754 covers root, api, and api/v2. Separate agent-shaped check files remain excluded.
- verified 2026-09-18: custom-agent frontmatter is `name` (required), `description`, and `model`, nothing else, on both the [doc page](https://raw.githubusercontent.com/aaif-goose/goose/main/documentation/docs/guides/context-engineering/custom-agents.md) ("Only `name` is required in the frontmatter. `description` and `model` are optional") and the shipping loader's `AgentMetadata` struct in [summon.rs](https://raw.githubusercontent.com/aaif-goose/goose/main/crates/goose/src/agents/platform_extensions/summon.rs), which carries exactly those three. There is no `deny_unknown_fields`, so a stray key is tolerated and ignored rather than rejected. That settled #864 against promoting `color` into the shared `.agents/agents/` renderer. Watch this struct, not just the table, when a new portable agent field is proposed for that tree.

## augment

- docs: https://docs.augmentcode.com/setup-augment/guidelines · https://docs.augmentcode.com/cli/subagents · /cli/config · /cli/rules · /cli/skills · /cli/hooks · /cli/custom-commands · /cli/integrations · https://docs.augmentcode.com/cli/permissions (repository `toolPermissions` in `.augment/settings.json`; append `.md` to any path for a markdown mirror)
- changelog: https://www.augmentcode.com/changelog
- watch: `.augment-guidelines` vs AGENTS.md, whether `.augment/rules/` exists. **The project-tier MCP question is settled and shipped** (#633, target-audit 2026-08-27): `/cli/config` names `<workspace>/.augment/settings.json` as "Best for team-shared project configuration, **such as shared MCP servers** or tool permissions", with `auggie mcp add --project` writing there, and the adapter now merges into it under `mcpServers`. As of 2026-09-04, `/cli/config` states this hierarchy in a numbered list rather than a "Supported By" table, but the settings-file section and the `mcpServers` example both still live entirely under the "Auggie CLI" docs, distinct from the VS Code / JetBrains extension's own Settings Panel GUI, so the CLI-only scope still holds. `<workspace>/.augment/commands/<name>.md` emits too (#630), preserving nested directories as command namespaces and the documented `description`, `argument-hint`, and `model` frontmatter. **Project hooks in this same settings.json are settled and shipped** (#629, 2026-09-10): `/cli/hooks` fresh-fetched that date confirms exactly five events (`PreToolUse`, `PostToolUse`, `Stop`, `SessionStart`, `SessionEnd`; the `hook_event_name` field also lists `Notification`, but it has no configuration section of its own, so it does not count as a sixth), `timeout` in **milliseconds** (default 60000 when absent, converted from the shared spec's seconds field), and a `command` field that must be a path ending in `.sh`/`.ps1`/`.cmd`/`.bat` or Augment never runs it - the one target in this registry with that requirement. The matcher vocabulary on `PreToolUse`/`PostToolUse` is Augment's own (`launch-process`, `str-replace-editor`, `save-file`, `remove-files`, ...), so a Claude matcher matches nothing; the three session events document matcher as unused, and the vendor's own example omits the key rather than writing it empty. Both `mcpServers` and `hooks` merge in the adapter's one `MergeJSONFile` call, not two, per the shape #718 fixed on qoder. **A trap, recorded so nobody "fixes" it** (target-audit 2026-09-19): `/cli/permissions` grew a "Migrating from legacy tool names" section marking `launch-process`, `view`, `str-replace-editor` and `save-file` as legacy aliases of `terminal`, `read`, `edit` and `write`. **That rename is scoped to the permissions surface only.** `/cli/subagents` and `/cli/hooks` still document the old names as current for `tools`, `disabled_tools` and hook matchers. Our code already splits the two vocabularies correctly. An auditor reading the permissions page alone would rename the hook matchers and break them, so check which page a name came from before touching either side.
- verified 2026-09-18: [permissions](https://docs.augmentcode.com/cli/permissions.md) confirms a committed `.augment/settings.json` taking `toolPermissions`, now emitted (#856). The shape is an ordered array, first match wins, and `permission` must be an object with a `type` field: "A rule with a bare-string permission is malformed and is dropped." Four types, `allow`, `deny`, `webhook-policy`, `script-policy`, with no prompt type, so the portable `ask` list raises a coverage note. Six tools (`terminal`, `read`, `edit`, `write`, `web-search`, `web-fetch`) and only `terminal` takes a matcher (`shellInputRegex`), so a path-scoped rule raises one too. MCP tools are named `{tool-name}_{server-name}`, truncated at 64 characters. Watch for a fifth permission type or a path matcher on `read`/`write`, either of which would retire a note.
- verified 2026-09-20: `eventType` is the second known-optional `toolPermissions` rule field, alongside `shellInputRegex`. Two values: `tool-call`, "(default) - Check before tool execution", and `tool-response`, "Check after execution but before returning results to agent". Omitting it keeps vendor-default timing, which is what this adapter writes, so nothing changes today. `x-augment` is the route if a rule ever needs the response phase.

## qoder

- docs: https://docs.qoder.com/user-guide/rules · https://docs.qoder.com/extensions/subagent · https://docs.qoder.com/extensions/skills · https://docs.qoder.com/cli/subagent · https://docs.qoder.com/cli/mcp-servers · https://docs.qoder.com/cli/Skills · https://docs.qoder.com/user-guide/chat/model-context-protocol · https://docs.qoder.com/cli/mcp-reference · https://docs.qoder.com/cli/hooks-reference · https://docs.qoder.com/cli/hooks · https://docs.qoder.com/cli/commands · https://docs.qoder.com/user-guide/commands · https://docs.qoder.com/cli/settings (the canonical page for `.qoder/settings.json`, and the source of the JSONC statement behind #725) · https://docs.qoder.com/cli/permissions (project `permissions.allow`, `permissions.deny`, and `permissions.ask`) · https://docs.qoder.com/cli/settings-reference (every `settings.json` key with type, default, and whether it needs a restart; the fastest way to spot a new block) · https://docs.qoder.com/cli/config-scope (which of the three files a key belongs in: `~/.qoder/settings.json`, `<project>/.qoder/settings.json`, `<project>/.qoder/settings.local.json`)
- changelog: https://docs.qoder.com/release-notes/qoder-cli.md and /release-notes/desktop.md (`qoder.com/changelog` is client-rendered and defeats both WebFetch and payload extraction; the docs release notes are plain markdown and split CLI from IDE)
- watch: `.qoder/rules/`, `.qoder/agents/`, `.qoder/skills/<name>/SKILL.md` (project scope; user scope is `~/.qoder/skills/{skill-name}/SKILL.md`, out of this adapter's reach), and `.qoder/commands/<name>.md` today, plus `.mcp.json`. Re-check on every audit whether `.agents/skills/` gains listing as a compatible path (#558 confirmed it does not yet, unlike kilo/augment/openhands). `docs.qoder.com/cli/subagent` confirms `tools` and `disallowedTools` both accept a comma-separated string or a string array (inline `[Read, Grep]` or YAML block-list), so the adapter's comma-separated emission is a choice, not a vendor limitation (target-audit 2026-08-08, #563); the CLI (`/cli/*`) and IDE (`/extensions/*`, `/user-guide/*`) doc trees cover the same features from two products, so both stay listed here. `docs.qoder.com/llms.txt` needs `curl --compressed` or the body is not valid UTF-8. Read `/cli/mcp-reference` rather than `/cli/mcp-servers` for MCP: it is the only page with the field tables, and it documents ten keys this adapter once reached by no route (`disabled`, `timeout`, `description`, `trust`, `includeTools`, `excludeTools`, `alwaysAllow`, `oauth`, stdio's `cwd`, and `qoder_url`, which arrived with the MCP Gateway in CLI 1.1.51 on 2026-09-12), which retired this repo's "not vendor-confirmed either way" claim about `disabled` (target-audit 2026-08-27). All ten reach the file: the first nine through explicit mappings, `qoder_url` through the `x-qoder` passthrough (`MergeCustomTargetMeta`, `qoder/mcp.go:172`), so the new key is a stale count here and not a gap (target-audit 2026-09-19). The adapter also moved off the shared `.mcp.json` to `<project>/.qoder/settings.json → mcpServers`, the second documented project-level location Claude Code never reads, so the two targets no longer contend for one path (#641). That file is also the hooks file, so #629 can merge into the same emitter. A `.mcp.json` from an older agnostic-ai still loads and outranks it; sync cannot sweep a JSON file (no provenance header) and the file may belong to claude, so the migration is manual. Hooks are 27 events, not the six a prior audit recorded nor the 23 #629 counted: `/cli/hooks-reference` is the authoritative list, and it carries `TaskCreated`, `TaskCompleted`, `TeammateIdle`, and `Setup` ("During initial installation") on top of the 23 (target-audit 2026-09-11, #737). Hook `timeout` is **seconds defaulting to 600**. Count the rows on `/cli/hooks-reference`, not `/cli/hooks`, since the two pages disagree on the event list. All 27 reach `hookLifecycle` and `hookEventsByTarget["qoder"]` as of #744. `/cli/hooks` also documents the `args` exec form ("The CLI runs the binary directly without a shell"), emitting since #746; that page states `shell` is ignored when `args` is set, and the adapter writes `shell` anyway with a field no-op note, so re-check the sentence if the vendor ever makes the pair an error. Commands (#630) confirmed `.qoder/commands/<command_name>.md` at project scope with `description` required and `name` cosmetic-only on `/cli/commands`, corroborated for the IDE too on `/user-guide/commands`; re-check the User-Level-wins precedence quirk `/cli/commands` documents on future audits, since the IDE page instead describes a "scope indicator" for a same-named pair rather than an override, and this adapter has no reach into `~/.qoder/commands/` either way.

- verified 2026-09-20: `/cli/subagent`'s "Local Subagent Full Field Reference" carries `memory`, values `user`, `project`, `local`, "Persistent memory scope for this Subagent. Only active when global automatic memory is enabled." That global switch is `autoMemoryEnabled` on `/cli/settings-reference`, a boolean defaulting to **`false`**, so the field is inert until a user opts in. **The table title is a trap**: "Local Subagent" means `kind: local`, the only value the same table's `kind` row accepts, not the user tier. It is the project-tier frontmatter reference. Also `/cli/mcp-reference`'s transport table gained an `sdk` row, "Built-in SDK-level server (in-process)", with no spec counterpart: an in-process server has no command and no URL to write, so there is nothing to emit. Watch for a file-configurable form of it.

## openhands

- docs: https://docs.openhands.dev/overview/skills · /overview/skills/path · https://docs.openhands.dev/openhands/usage/settings/mcp-settings · /openhands/usage/customization/hooks · /openhands/usage/customization/repository · /sdk/guides/agent-file-based (append `.md` to any path for a markdown mirror)
- changelog: https://github.com/OpenHands/OpenHands/releases (the org renamed; the recorded `All-Hands-AI/OpenHands` URL 301s here and still resolves, corrected 2026-09-11, #737)
- watch: `.agents/skills/` shared tree. The microagents item is **resolved**: the vendor now heads that section "Skills (formerly Microagents)" and says "use `.agents/skills/` for new skills", which is our default. Path-triggered rules are now declared too (#643), folder form at `.agents/skills/<name>/SKILL.md`, sidestepping the loose-`.md`-in-a-shared-tree hazard a prior audit blocked on. `.openhands/hooks.json` emits since #700 in the vendor-documented Claude-compatible PascalCase wrapper, with a coverage note for OpenHands' own matcher vocabulary (`file_editor`, `terminal`). File-based agents now emit to the primary `{project}/.agents/agents/*.md` path (#631), auto-registered from `_ensure_agent_ready()` for `LocalConversation`. The loader skips subdirectories, so Antigravity can coexist in the same root at `<name>/agent.md` (#717). Watch whether Cloud documents the same automatic registration.
- verified 2026-09-18: [file-based agents](https://docs.openhands.dev/sdk/guides/agent-file-based.md) documents `color` on a project agent, but it stays unemitted with a coverage note (#864). Goose reads the same `.agents/agents/` tree and has no such key (see the goose section), so the shared renderer would write a key one of the two readers ignores. `x-openhands.color` is the escape hatch. Re-open this only if Goose adds `color` or OpenHands stops sharing the path.

## factory

- docs: https://docs.factory.ai/harness/subagents · /harness/skills · /harness/hooks · /harness/custom-slash-commands · /harness/mcp · /droid-cli/settings (the `settings.json` **key** reference: every key, its options, and its default. Its own location table lists `~/.factory/settings.json` alone. **Do not read that as a user-tier-only file and stop**, which is how this pairing hid the missing project-tier emitter for three runs) · /enterprise/hierarchical-settings-and-org-control (**the page that settles the project tier, and it is real**: "Settings are authored in `.factory/` folders, using the **same schema** at every level", with the levels table row "| **Project** | `<git-root>/.factory/` | Repo maintainers |" and "Each `.factory/` folder can contain: - `settings.json`: general settings (models, safety, preferences, telemetry)." Corroborated on /harness/skills: "The **User** tab writes the choice to `disabledSkills` in `~/.factory/settings.json`; the **Project** tab writes to `<project>/.factory/settings.json`." Read this page for scope and `/droid-cli/settings` for keys, never the reverse. It carries the org-managed schema and the precedence order too, and the org half stays out of our reach) · /enterprise/llm-safety-and-agent-controls (**the only page with the command-pattern examples**, and the one that names the sandbox's extra network keys) · https://docs.factory.ai/autonomy-and-safety/sandbox (the `sandbox.*` full reference; the hierarchical page is captioned "Where `sandbox.*` lives") · https://docs.factory.ai/ (append `.md` to any path for a markdown mirror)
- changelog: https://docs.factory.ai/changelog/release-notes.md (not linked from the docs nav, but listed in `docs.factory.ai/llms.txt` under `## Changelog`)
- watch: **`/droid-cli/settings` is not the whole settings story.** Its own "Where settings live" table lists `~/.factory/settings.json` alone, which reads as a stop sign against the standing rule to skip user-tier paths. The project tier is one page over, on `/enterprise/hierarchical-settings-and-org-control`: "Settings are authored in `.factory/` folders, using the same schema at every level", levels table row "**Project** | `<project-root>/.factory/`", and "Each `.factory/` folder can contain: `settings.json`". `/harness/skills` corroborates it: "the **Project** tab writes to `<project>/.factory/settings.json`". That file now emits a portable `model` (target-audit 2026-09-19, #891). **That watch fired on 2026-09-20 (#948): the vendor now publishes both the grammar and the difference.** Grammar is shell-command patterns, and both spellings appear in vendor examples: bare (`["ls", "pwd", "dir"]`, `/droid-cli/settings`) and prefix-glob (`["npm *", "sudo *", "rm -rf *"]`, `/enterprise/llm-safety-and-agent-controls`). Scope is project-tier and union-merged across levels. **The semantics are the trap: mapping portable `deny` onto `commandDenylist` by name similarity is wrong.** Factory's denylist is prompt-then-approve ("A denied command can still be run if you explicitly approve it"), so portable `ask` maps to `commandDenylist` and portable `deny` maps to `commandBlocklist` ("there is no prompt and no way to approve them"). The denylist's prompt behavior is asserted positively on three pages, not merely implied by the blocklist's contrast clause. Precedence: "Commands that appear in both the allowlist and denylist default to the denylist behavior. The blocklist always takes precedence over both." Also watch `.factory/droids/` frontmatter keys, and whether MCP config became project-scoped. `docs.factory.ai/cli/configuration/custom-droids`, the prior URL here, 308s to the page above (Harness rebrand, target-audit 2026-08-08, #563). The `tools` array is **not** a Claude-style pass-through, whatever `spec-format.md` says: the vendor's closed table is `Read`, `LS`, `Grep`, `Glob`, `Create`, `Edit`, `ApplyPatch`, `Execute`, `WebSearch`, `FetchUrl`, and "Unknown IDs cause a validation error", so `Bash`/`Write`/`WebFetch` need translating to `Execute`/`Create`/`FetchUrl` (target-audit 2026-08-27). Factory ships its own Claude-name mapper for exactly this reason, and documents three more load-time rules: `TodoWrite` and `Skill` are implicit and must not be listed, `ExitSpecMode`/`GenerateDroid` are validation errors, and `tools: all` is rejected. Skills (`.agents/skills/**/SKILL.md`, a tree ten other targets already write, so the dedupe absorbs it) are now declared (#632); the doc's other compatibility path, `.agent/skills/**/SKILL.md`, stays unwritten since the shared tree already covers it. `.factory/hooks.json` now emits too (#629): keyed directly by event with no `hooks` wrapper, nine events (`PreToolUse`, `PostToolUse`, `UserPromptSubmit`, `Notification`, `Stop`, `SubagentStop`, `PreCompact`, `SessionStart`, `SessionEnd`), `timeout` in seconds (vendor default 60 when absent). `.factory/commands/<name>.md` now emits explicit command specs with `description` and `argument-hint` frontmatter while preserving `$ARGUMENTS` (#630). Factory still recommends Skills for new reusable workflows, so watch whether the legacy command loader is removed. **Sandbox (2026-09-20, #949):** `sandbox.*` is a real project-tier key in the same `.factory/settings.json` (`enabled`, `mode`, `filesystem.allowWrite`/`denyWrite`/`denyRead`, `network.allowedDomains` plus `allowUnixSockets`/`allowAllUnixSockets`/`allowLocalBinding`/`httpProxyPort`/`socksProxyPort`). It maps onto no portable kind and should not get one: kernel enforcement rather than approval gating, enabling it moves the default baseline for unlisted paths, `denyWrite` overrides `allowWrite`, there is no `ask` tier, and `allowedDomains` is egress filtering with no counterpart. Kilo has a sandbox too and the two overlap only on `enabled`. It is unreachable today because the settings kind has no `x-<target>` passthrough; that is the gap, not a missing spec field. **Also 2026-09-20:** `/harness/skills` now names the vendor's own project tier, "| Project | `<repo>/.factory/skills/<skill-name>/SKILL.md` |", with `.agents/skills/**/SKILL.md` demoted to "Compatibility". Our shared-tree default still lands on a documented path, so this is framing, not drift. And the sandbox blocks hooks when isolation is unavailable: "if the sandbox cannot be established, the hook is blocked instead of running on the host".
- verified 2026-09-12: [MCP field reference](https://docs.factory.ai/harness/mcp.md) confirms disabledTools, timeout and connectTimeout in milliseconds, plus remote oauth:false or an object with scopes, resource, authorizationServerIssuer, clientId, clientSecret, clientMetadataUrl, tokenEndpointAuthMethod, and callbackPort (#774). These fields now emit through a Factory-only mapping with x-factory overrides.

## kilo

- docs: https://kilo.ai/docs (client-rendered; use WebFetch) · /docs/customize/custom-rules · /docs/customize/agents-md · /docs/customize/agent-permissions (the `permission` map `x-kilo` reaches: actions, patterns and precedence, **no tool names**) · /docs/automate/tools (the closed tool vocabulary, mirrored at `packages/kilo-docs/pages/automate/tools/index.md`) · /docs/getting-started/settings/auto-approving-actions (the same names as `permission` keys in `kilo.jsonc`) · /docs/code-with-ai/agents/goals (the reserved `goal` agent name, #736) · /docs/customize/custom-subagents · /docs/customize/skills (the `skills.paths` / `skills.urls` config keys, the `/.github/skills` absolute-then-project-relative fallback, and the three default skill trees, target-audit 2026-09-18 #861, #865; mirrored at `Kilo-Org/kilocode`'s `packages/kilo-docs/pages/customize/skills.md`) (raw source also mirrored at `Kilo-Org/kilocode`'s `packages/kilo-docs/pages/customize/custom-subagents.md`, useful when the rendered site defeats fetching; appending `.md` to a kilo.ai docs path used to serve that raw source directly but now 404s there, confirmed 2026-08-09, #590, so use the GitHub mirror instead). Two dead ends worth not re-walking (2026-09-19): `kilo.ai/docs/automate/tools.md` 404s, and so does the flat mirror `packages/kilo-docs/pages/automate/tools.md`. The file is `packages/kilo-docs/pages/automate/tools/index.md`, so try the `index.md` form before concluding a kilo page is gone.
- changelog: https://github.com/Kilo-Org/kilocode/releases (head **v7.7.5**, 2026-09-18, read 2026-09-19. v7.6.1 and v7.6.2 both shipped on 2026-09-10 and neither touched a file surface we emit: v7.6.1 was webview rendering plus an `@` model-mention picker, while v7.6.2 added a `KILO_EXPERIMENTAL_SHARED_AGENT_BOARD` env var next to the then-current `experimental.shared_agent_board` config key. v7.7.2 made the shared board stable and enabled by default under top-level `shared_agent_board`; `experimental.shared_agent_board` is no longer read. GitHub mirror paths worth citing directly, since kilo.ai defeats fetching: `packages/kilo-docs/pages/getting-started/settings/index.md`, `/customize/workflows.md`, `/customize/agent-permissions.md`, `/code-with-ai/agents/goals.md`, `/automate/mcp/using-in-kilo-code.md`)
- **read the shared-board releases in order, not first-hit** (2026-09-20). v7.7.0 and v7.7.1, both 2026-09-15, still name `experimental.shared_agent_board`. Only v7.7.2, published the same day, moves it to top-level `shared_agent_board` and states the experimental key is no longer read. Three releases on one date name one feature under two keys, so an auditor who stops at the first hit records the superseded one. v7.6.2's `KILO_EXPERIMENTAL_SHARED_AGENT_BOARD` is superseded the same way.
- watch: `kilo.jsonc` `mcp` shape (already confirmed distinct from the deprecated `mcpServers`), `.kilo/agents/` vs custom modes, whether `.kilocode/rules/` backward compatibility is dropped. `instructions` array entries: this adapter lists one explicit `.kilo/rules/<name>.md` path per rule rather than a `.kilo/rules/*.md` glob (unconfirmed whether a `**` recursive glob would also cover a scoped rule); revisit if the vendor doc ever confirms glob recursion. Agent Configuration Options table (target-audit 2026-08-08, #562): `color`/`mode` now read from the generic top-level field; `disable`/`hidden`/`steps`/`temperature`/`top_p` are a deliberate `x-kilo`-only decision, not a gap, unless a future revisit finds a cross-tool counterpart worth promoting one of them to. **A priority sentence appeared for the project config path** (target-audit 2026-08-27): `getting-started/settings/index.md` now reads "`kilo.jsonc` in your project root, or `.kilo/kilo.jsonc` for a cleaner setup. **The `.kilo/` version takes priority if both exist.**" This adapter writes the root file, which is the loser of that sentence, and that one file carries both the `mcp` map and the `instructions` array. Not breaking today, since a user with no `.kilo/kilo.jsonc` is unaffected — but the vendor now recommends creating one. Report it as "our target is the loser of a new priority sentence", not as "the preference flipped". Resolved (target-audit 2026-09-03, #644): the same page has since grown a "Config File Precedence" section naming an explicit 8-level system (Legacy Kilocode < Remote well-known < Global < Custom < Project `kilo.jsonc` < `.kilo/` directory < Inline environment < Managed/Enterprise) and states "Higher-priority levels override lower ones", confirming a merge across named sources, not an exclusive first-match read of one whole file. Fixed with a doc caveat on the adapter and in targets.md rather than a path change, since the merge model means an untouched key on the root file still reaches Kilo Code even when `.kilo/kilo.jsonc` exists; only a key `.kilo/kilo.jsonc` itself redeclares gets shadowed. `.kilo/commands/<name>.md` is documented with frontmatter near-identical to OpenCode's; emitted since #630, with `variant` as the one extra key beyond OpenCode's own set. **Reserved command names** (target-audit 2026-09-11, #736): `code-with-ai/agents/goals` reserves `goal` as of v7.6.0, and the emitter now notes a clash instead of writing a file Kilo rejects in silence. Watch that page for a second reserved name. **That watch item is retired** (target-audit 2026-09-19): it sent auditors to `customize/agent-permissions` for a documented tool-name vocabulary, and that page never lists tool names. It covers actions, patterns and precedence only, so an auditor who checked it read "still not documented" and moved on for three runs. The vocabulary is published, on two other pages. `automate/tools/index.md` gives the closed set by group: "**Read Group** ... `read`, `glob`, `grep`", "**Edit Group** ... `edit`, `write`, `apply_patch`", "**Execute Group** ... `bash`", "**Web Group** ... `webfetch`, `websearch`". `getting-started/settings/auto-approving-actions` tables the same names as `permission` keys ("Permissions are configured under the `permission` key in `kilo.jsonc`"), adding `external_directory`, `task`, `skill`, `lsp`, `todoread`/`todowrite` and `doom_loop`. `customize/agent-permissions` keeps one job: the ordering rule, "Put broad fallbacks first and exceptions after them." `customize/skills.md` documents three default project skill trees (`.kilo/skills/`, `.agents/skills/`, `.claude/skills/`) plus `skills.paths` / `skills.urls` in `kilo.jsonc`; a `skills-dir` outside those three now writes a `skills.paths` entry (target-audit 2026-09-18, #861). Watch that tree list for a fourth name, since each one added is one fewer config entry needed. The shared board is stable and enabled by default under top-level `shared_agent_board` as of v7.7.2. Watch for changes there, not under `experimental.shared_agent_board`, which is no longer read. Both the settings map and agent `tools` now translate (#890).
- verified 2026-09-12: [Ignore reference](https://kilo.ai/docs/customize/context/kilocodeignore) confirms that the compatibility migrator converts .kilocodeignore to read/edit permission denials; the adapter emits and imports the native input without inventing permission translation (#773). [MCP documentation source](https://github.com/Kilo-Org/kilocode/blob/main/packages/kilo-docs/pages/automate/mcp/using-in-kilo-code.md) confirms timeout in milliseconds on both transports and remote oauth:false, now reachable directly or through x-kilo (#775). Broader OAuth objects remain excluded.
