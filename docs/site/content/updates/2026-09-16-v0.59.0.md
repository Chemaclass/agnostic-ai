+++
title = "agnostic-ai v0.59.0: detect stale generated files in CI"
description = "`agnostic-ai verify` makes stale generated files fail CI. Agents, hooks, skills, and environment specs also reach more targets."
date = 2026-09-16T00:00:00+02:00
slug = "2026-09-16-v0.59.0"
aliases = ["updates/2026-09-16-v0.59.0.html"]

[extra]
kind = "release"
version = "v0.59.0"
dek = "`agnostic-ai verify` detects stale generated files in CI. It also records the harness identity, so a change to the system that produced them is visible in review."
rss_guid = "https://agnostic-ai.org/updates/2026-09-16-v0.59.0.html"
archive_stats = "11 product changes · 5 site updates · 4 upstream notes"
targets = ["amp", "antigravity", "augment", "claude", "cline", "codex", "continue", "copilot", "cursor", "factory", "goose", "junie", "kilo", "kiro", "opencode", "openhands", "qoder", "warp", "windsurf", "zed"]

[[extra.signals]]
status = "shipped"
targets = ["agnostic-ai"]
title = "CI detects stale generated files"
summary = "`agnostic-ai verify` fails a build when generated output no longer matches its sources."

[[extra.signals]]
status = "shipped"
targets = ["Amp", "Goose", "OpenHands"]
title = "More targets receive project configuration"
summary = "Agents, hooks, and environment setup now reach more native tool surfaces."
+++

## What to do {#actions}

1. Add `agnostic-ai verify` to CI.
2. If you use Antigravity, run `agnostic-ai sync` once to move managed agents to its current layout.

## What changed {#changed}

- CI can now reject stale generated files before they merge.
- Project agents and environment configuration reach Amp, Goose, and OpenHands.
- Imports retain more native MCP and skill paths.

[Read the complete v0.59.0 release notes](https://github.com/Chemaclass/agnostic-ai/releases/tag/v0.59.0).
