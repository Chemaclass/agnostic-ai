+++
title = "agnostic-ai v0.61.0: Cline rules reach Cline"
description = "Cline rules now emit to its active path. Sync also rejects invalid names and keeps moved Claude Code directories round-trippable."
date = 2026-09-18T17:10:00+02:00
slug = "2026-09-18-v0.61.0"
aliases = ["updates/2026-09-18-v0.61.0.html"]

[extra]
kind = "release"
version = "v0.61.0"
dek = "Cline rules now emit to `.clinerules/`, the path Cline reads. Projects using the Cline target should sync once and review the generated move."
rss_guid = "https://agnostic-ai.org/updates/2026-09-18-v0.61.0.html"
archive_stats = "14 product changes · 3 site updates · 3 upstream notes"
targets = ["antigravity", "augment", "claude", "cline", "copilot", "cursor", "factory", "goose", "junie", "kilo", "opencode", "qoder", "warp", "windsurf"]

[[extra.signals]]
status = "shipped"
targets = ["Cline"]
title = "Cline rules use the path Cline reads"
summary = "Rules move from `.cline/rules` to `.clinerules/` on the next sync."

[[extra.signals]]
status = "shipped"
targets = ["OpenCode", "Junie"]
title = "Invalid names fail before generation"
summary = "A vendor-rejected agent or skill name now stops sync instead of producing an ignored file."
+++

## What to do {#actions}

1. If you sync `cline`, run `agnostic-ai sync` and review the move to `.clinerules/`.
2. If you sync Claude and Cursor together, scope shared hooks to avoid Cursor running them twice.

## What changed {#changed}

- Cline rules now reach the path its CLI and editors read.
- Invalid OpenCode and Junie names fail before an unusable file is written.
- Moved Claude Code output directories round-trip through `import` and `doctor`.

[Read the complete v0.61.0 release notes](https://github.com/Chemaclass/agnostic-ai/releases/tag/v0.61.0).
