+++
title = "agnostic-ai v0.63.0: settings specs take an x-<target> block"
description = "A settings spec carries a target's own keys into that target's settings file, the merge stops deleting the translated policy it collides with, and a wrong-shaped hatch value now says so instead of going missing."
date = 2026-09-20T19:30:00+02:00
slug = "2026-09-20-v0.63.0"
aliases = ["updates/2026-09-20-v0.63.0.html"]

[extra]
kind = "release"
version = "v0.63.0"
dek = "A settings spec now carries a target's own keys, so `x-factory.sandbox` reaches `.factory/settings.json` instead of being dropped. On a key this tool also writes, the two values merge, so a translated deny rule no longer vanishes because you added one of your own."
rss_guid = "https://agnostic-ai.org/updates/2026-09-20-v0.63.0.html"
archive_stats = "18 product changes · 6 site updates · 0 upstream notes"
targets = ["amp", "augment", "claude", "copilot", "cursor", "factory", "gemini", "junie", "kilo", "kiro", "opencode", "qoder", "windsurf"]

[[extra.signals]]
status = "shipped"
targets = ["Factory", "Kilo Code", "Amp", "Qoder", "Copilot"]
title = "Settings specs take an x-<target> block"
summary = "A key this project declines to model, Factory's sandbox among them, reaches that target's settings file instead of being dropped without a word."

[[extra.signals]]
status = "shipped"
targets = ["Claude", "Factory", "Qoder", "Augment"]
title = "A hatch key merges with the policy it collides with"
summary = "Lists union and objects merge, so a translated deny rule survives an x-<target> key beside it. Shipped deleting it in v0.62.0."

[[extra.signals]]
status = "shipped"
targets = ["Augment", "Windsurf", "Kilo Code", "OpenCode"]
title = "A wrong-shaped hatch value stops going missing"
summary = "It used to read as absent: the translated policy shipped instead and nothing said so. It now raises a coverage note naming the key and both shapes."

[[extra.signals]]
status = "shipped"
targets = ["Claude", "Qoder", "Junie", "Factory"]
title = "effort takes a per-target map"
summary = "effort: {claude: xhigh, qoder: 8000, default: high} resolves per target, where the map form used to write a broken nested mapping into three agent files."
+++

## What to do {#actions}

1. **Wrote an `x-<target>` key in a settings spec on v0.62.0?** Run `agnostic-ai sync` and read the diff before committing. A key this tool also writes used to be replaced by yours; now the two merge, so the file gains the translated entries it was losing. A permission list is the case to check first.
2. **Used `x-qoder.mcpServers` or `x-augment.mcpServers` to replace the whole server map?** That no longer works, and it was never reliable. Entries union by server name, and a name both sides carry is taken from your block whole. Nothing to do unless you were relying on the replacement (a read of one file).
3. **Wrote `x-augment.toolPermissions`, `x-windsurf.permissions`, `x-kilo.permission` or `x-opencode.permission`?** Read the coverage notes on your next sync. A value of the wrong shape used to read as absent and ship the translated policy silently; it now names the key and both shapes, so a policy you thought was live may turn out never to have been (one sync, then fix any note).
4. **Installing from a shared IP, CI runner or office network?** Take this release. `scripts/install.sh`, `scripts/install.ps1`, `agnostic-ai upgrade` and the npm wrapper stopped calling `api.github.com`, whose 60 unauthenticated requests an hour per IP failed installs with a bare HTTP 403.

Nothing else needs action. A spec with no `x-<target>` block and no `effort` map emits the same bytes as v0.62.0.

## Shipped in agnostic-ai v0.63.0 {#shipped}

### Added

- An agent's `effort` takes a per-target map the way `model` already does: `effort: {claude: xhigh, qoder: 8000, default: high}` resolves per target instead of writing a broken nested mapping into the Claude, Qoder, and Junie agent files (#968).
- Settings specs take an `x-<target>` block, so a target-specific key such as `x-factory.sandbox` reaches that target's settings file instead of being dropped without a word (#949).
- Amp accepts settings specs instead of refusing them: `x-amp` keys such as `amp.tools.disable` merge into `.amp/settings.json`, and the portable allow, deny, ask, and model fields each report why Amp has nowhere to put them (#950).
- Factory writes the portable permission policy to `commandAllowlist`, `commandDenylist`, and `commandBlocklist` in `.factory/settings.json`. Portable `ask` maps to the denylist, which prompts, and portable `deny` maps to the blocklist, which cannot be approved; rules outside `Bash` still raise a coverage note (#948).
- An agent's `memory` scope now reaches Qoder at `.qoder/agents/<name>.md`, the file its subagent reference documents the field on, with the same `user`/`project`/`local` values (#953).
- `agnostic-ai import kiro` now reads `.kiro/hooks/*.json`, so a repo synced to Kiro round-trips its own hooks; entries differing only in `action.command` recombine into one spec (#952).
- The npm package publishes with a provenance attestation, so its page links the tarball back to the exact commit and workflow run; a signing outage downgrades to a plain publish instead of failing the release (#937).

### Changed

- The release pushes the Homebrew cask with a token minted per run from a GitHub App scoped to the tap, so the credential stops expiring; it falls back to the stored `HOMEBREW_TAP_TOKEN` until the App exists (#943).
- The npm package points its homepage at agnostic-ai.org instead of the GitHub readme, and widens its keywords from nine to twenty so npm search surfaces it (#937).

### Fixed

- An `x-<target>` settings key no longer deletes the translated policy it collides with: lists union and objects merge, so a portable `deny` rule survives an `x-factory.commandBlocklist` or `x-claude.permissions.deny` beside it, and a shape that cannot merge prints a coverage note (#966). A map of whole records is the exception: an `x-qoder.mcpServers` or `x-augment.mcpServers` entry naming a server the MCP specs already emitted replaces that server whole, so one entry can no longer carry your `command` beside the spec's `args`, or a `url` beside a `command` (#974).
- An `x-qoder.hooks` or `x-augment.hooks` block now merges with the generated hook block event by event, in the vendor's lifecycle order, instead of replacing every translated hook under a note calling two objects a shape conflict (#976).
- A wrong-shaped `x-augment.toolPermissions`, `x-windsurf.permissions`, `x-kilo.permission` or `x-opencode.permission` now raises a coverage note naming the key and both shapes, instead of reading as absent and shipping the translated policy in silence (#976).
- An integer `effort` on an agent now raises Factory's coverage note instead of vanishing in silence: `effort: 8000` is reported as outside `low`, `medium`, `high`, the way `xhigh` already was (#968).
- An `mcp__<server>__<tool>` permission rule now reaches `opencode.json` as `<server>_<tool>`, the key OpenCode registers that tool under, instead of dropping behind a coverage note claiming the vendor has no MCP key (#947).
- A `WebSearch` permission rule now reaches `.devin/config.json` as `web_search`, which Devin has accepted in all three lists since CLI v3000.10.21, and `import windsurf` reads it back (#951).
- Installing the latest release no longer dies on a shared IP. `scripts/install.sh`, `scripts/install.ps1` and `agnostic-ai upgrade` follow the `github.com/.../releases/latest` redirect instead of calling `api.github.com`, whose 60 unauthenticated requests an hour per IP failed installs with HTTP 403. The npm wrapper reads its latest version from that same `releases/latest` route, and a throttled request now says it was rate limited instead of printing a bare 403 (#940).
- The npm wrapper survives the four ways its download used to fail: `AGNOSTIC_AI_VERSION=0.61.0` installs the same binary as `v0.61.0` rather than building a 404 URL, an empty `AggregateError` prints a real message instead of the bare line `agnostic-ai:`, the request times out after 30 seconds of silence and stops after 5 hops, and a failed `tar` extraction names the archive and points at the install script or `go install`. The bin shim exits `128 + signal` so a killed run is distinguishable (#936).
- The release's distribution guard retries `npm view` and the Homebrew contents API with backoff, so a registry replica that lags the publish by seconds no longer reports a good release as failed (#937).
- `brew` stops printing `Calling postflight is deprecated` for our cask: the release emits Homebrew's `postflight_steps` stanza instead of the raw hook, and the quarantine strip still runs (#933).
- `agnostic-ai update` only reports a PATH copy that actually wins the lookup, instead of telling you to delete a stale binary that sits later on PATH and shadows nothing.

### Site

- The hero stops claiming a rule becomes `CLAUDE.md`, which carries none of a rule's body. It shows one agent spec and the five native files it really produces, pickable: `.codex/agents/reviewer.toml` is TOML with no `tools`, Cursor drops `tools`, Gemini translates the names, and every path and body is copied from a real `sync`. The install command sends a CSS pulse down four labelled rails to the targets it writes and a fifth counting the rest, `+21 more`, derived from the capability data rather than typed. The fan draws itself once and then stays: the names and their endpoint dots hold at full opacity for good while a pulse keeps travelling the rails behind them, and it rests as a faint finished fan under `prefers-reduced-motion` (#967).
- The landing reads lighter on a phone. The six-column capability matrix, thirty labelled rows at that width, collapses to the five target names as chips with the full matrix one tap away, and its `Native`/`opt-in` legend hides with it. The agent-setup shortcut is the link alone, without the sentence or the rule above it. The primary nav holds only pages of this site: GitHub moves out of it and in beside search and the theme toggle, as a mark rather than a word, and every link that leaves the site now opens in a new tab with `rel="noopener noreferrer"`, the repository, the release tag beside the wordmark and the footer's GitHub and sponsor links among them.
- The talk demo moves off the landing page to the foot of the documentation index, at `/docs/#demo`, where a reader already looking for it will find it. Its copy moves into the docs section frontmatter, its `VideoObject` metadata moves with it, and the click-to-play facade ships as `video.js` so the landing stops carrying a player it no longer shows.
- The spec-format page merges "Per-target models" and "`effort` support by target" into one section at `#per-target-model-and-effort`, with a worked example showing both keys and a "How it lands" column that stops implying agnostic-ai transforms anything on Claude Code or Cursor. The Claude, Cursor, Factory and Qoder pages link the new anchor, and `spec.schema.json` finally types `model` as a map as well as a string (#968). It also names all four targets that report a whole permission policy as unreachable, the Codex page says a portable `permissions` list raises a note pointing at `outputs.codex.exec-policies`, and the alternatives page is titled `Why agnostic-ai instead of symlinks`.
- Four target pages drop a claim the vendor does not make. Copilot names `deniedUrls`, its one repository-tier deny route, reachable with `x-copilot.deniedUrls`, rather than implying none exists (#959), and says Copilot CLI reads `.claude/settings.json` for a five-key repository subset, so `enabledPlugins` set for claude also sets Copilot policy (#956). Claude marks `taskOutputMaxChars` deprecated, since v2.1.277 removed its effect and the key still emits because the `stable` dist-tag predates that release (#955). Cursor says it loads skills from `.claude/skills` and `.codex/skills` too, read from three roots with precedence undocumented (#957). Claude, Cursor and Copilot stop calling MCP `roots` a documented per-server key; no vendor names one, and the field still emits as passthrough.
- Installation lists the two package managers that work today, `brew install --cask Chemaclass/tap/agnostic-ai` and `npm install -g agnostic-ai`, and names winget and Scoop as the two that do not. The readme and the plugin install skill offer npm as a route instead of warning that it is unpublished, the npm readme links the site and drops its pin on a seventeen-release-old tag (#920), and the current release now shows beside the wordmark on every page, linking to its GitHub release.

## Upstream CLI and model news {#upstream}

v0.62.0 shipped on 2026-09-19 and this release is one day later. No vendor change in that window was verified against primary documentation, so there is nothing to report here rather than filler. The [target audit](https://github.com/Chemaclass/agnostic-ai/issues?q=label%3Atarget-audit) runs on its own schedule and files what it confirms.
