+++
title = "agnostic-ai v0.60.0: sync stops hiding project agent memory"
description = "Sync now keeps shareable Claude Code agent memory visible to Git. It also fixes configured output paths and adds a demo memory-curator skill."
date = 2026-09-18T00:00:00+02:00
slug = "2026-09-18-v0.60.0"
aliases = ["updates/2026-09-18-v0.60.0.html"]

[extra]
kind = "release"
version = "v0.60.0"
dek = "Claude Code's shareable `agent-memory/` is no longer hidden by the managed `.gitignore` block. Run one sync, then decide whether to commit or ignore the files now visible to Git."
rss_guid = "https://agnostic-ai.org/updates/2026-09-18-v0.60.0.html"
archive_stats = "5 product changes · 5 site updates · 6 upstream notes"
targets = ["antigravity", "claude", "cline", "codex", "crush", "gemini", "goose", "kilo", "openhands", "qoder"]

[[extra.signals]]
status = "shipped"
targets = ["Claude Code"]
title = "Shareable agent memory is visible to Git"
summary = "The managed ignore rules now keep project memory visible and ignore the machine-local store."

[[extra.signals]]
status = "shipped"
targets = ["Claude Code", "Crush"]
title = "Configured paths stay inside the configured directory"
summary = "Rules, commands, and imported hook names no longer escape their intended output path."
+++

## What to do {#actions}

1. Run `agnostic-ai sync`, then review `git status` for `.claude/agent-memory/`.
2. If you override `outputs.claude.dir`, review the moved generated files after sync.

## What changed {#changed}

- Shareable Claude Code agent memory is no longer hidden from version control.
- Configured output directories now move their rules and commands with them.
- `agnostic-ai init --demo` adds a memory-curator skill for reviewing a store before changing it.

[Read the complete v0.60.0 release notes](https://github.com/Chemaclass/agnostic-ai/releases/tag/v0.60.0).
