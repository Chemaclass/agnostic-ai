+++
title = "Documentation"
description = "Install agnostic-ai, write portable specs, and keep every tool's generated config in sync."
sort_by = "weight"
template = "docs/section.html"
page_template = "docs/page.html"
+++

Write specs once under `.agnostic-ai/`, run `agnostic-ai sync`, and each tool gets its own config. Start fresh, or import an existing setup and keep its behavior.
