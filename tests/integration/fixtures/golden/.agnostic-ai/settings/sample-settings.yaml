permissions:
  allow:
    - Read(**)
    - Bash(go test:*)
  deny:
    - Bash(rm:*)
    - Bash(rm -rf /)
    - WebSearch
    - mcp__github__create_issue
  ask:
    - Write(.env*)
model: example-model
x-factory:
  commandBlocklist:
    - author-only
x-claude:
  permissions:
    deny:
      - AuthorOnly
